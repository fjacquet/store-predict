---
phase: 07-ui-bug-fixes-and-report-enhancements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store_predict/ui/components/vm_table.py
  - src/store_predict/ui/pages/review.py
  - src/store_predict/ui/components/workload_dialog.py
autonomous: true
requirements: [FR-4.1, FR-4.2, FR-4.3, FR-4.4, FR-4.5, FR-4.6]

must_haves:
  truths:
    - "Multi-row selection works in AG Grid with header checkbox"
    - "Active filters are preserved after workload modification"
    - "Current page position is preserved after workload modification"
    - "Subcategory can be selected when changing workload category"
    - "Unknown (Reducible) VMs are editable -- user can reassign workload and DRR"
    - "Row click opens multi-select dialog, cell edit does inline dropdown"
  artifacts:
    - path: "src/store_predict/ui/components/vm_table.py"
      provides: "AG Grid with multiRow selection, subcategory column"
      contains: "multiRow"
    - path: "src/store_predict/ui/pages/review.py"
      provides: "Filter/page preservation logic, subcategory cascading"
      contains: "getFilterModel"
    - path: "src/store_predict/ui/components/workload_dialog.py"
      provides: "Category+subcategory cascading selection"
      contains: "subcategory"
  key_links:
    - from: "src/store_predict/ui/pages/review.py"
      to: "src/store_predict/ui/components/vm_table.py"
      via: "create_vm_table call with callbacks"
      pattern: "create_vm_table"
    - from: "src/store_predict/ui/pages/review.py"
      to: "AG Grid JS API"
      via: "run_grid_method for filter/page state"
      pattern: "getFilterModel"
---

<objective>
Fix AG Grid interaction bugs: multi-row selection, filter/page preservation after edits, subcategory selection in dropdown, and Unknown editability.

Purpose: Current UI has broken multi-select, loses filter/page state on edits, and only allows category (not subcategory) selection. These are the most impactful UX bugs.

Output: Working AG Grid with proper selection, state preservation, and cascading category/subcategory editing.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store_predict/ui/components/vm_table.py
@src/store_predict/ui/pages/review.py
@src/store_predict/ui/components/workload_dialog.py
@.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AG Grid multi-select and add subcategory inline editing</name>
  <files>
    src/store_predict/ui/components/vm_table.py
  </files>
  <action>
    1. Fix multi-row selection: Change rowSelection from {"mode": "singleRow"} to:
       {"mode": "multiRow", "headerCheckbox": True, "enableClickSelection": False}
       NOTE: enableClickSelection must be False because row clicks open the workload dialog. Selection happens via checkboxes only.

    2. Add a checkbox column as the first column:
       {"headerCheckboxSelection": True, "checkboxSelection": True, "width": 50, "maxWidth": 50, "suppressMenu": True}
       Actually per AG Grid 32+, the checkbox column is automatic with multiRow + headerCheckbox. Remove explicit checkbox column if multiRow handles it. Test both approaches -- if automatic checkboxes don't appear, add explicit column.

    3. Change the workload_category cellEditor from agSelectCellEditor (category only) to a custom approach that includes subcategory:
       - Use agSelectCellEditor but with values being the full "Category / Subcategory" labels from workload_options
       - Add a new parameter subcategory_labels: list[str] to create_vm_table
       - The cellEditorParams values will be these full labels
       - This lets users pick the exact subcategory inline

    4. Ensure workload_category column and workload_subcategory column are BOTH editable for ALL rows including "Unknown (Reducible)". The existing editable: True already covers this, but verify there's no filtering condition. Unknown rows must be editable.

    5. Add getRowId to grid options to enable stable row identity after updates:
       "getRowId": "params => params.data.vm_name"
  </action>
  <verify>
    rtk ruff check src/store_predict/ui/components/vm_table.py
    Manual: run app, verify multi-row checkboxes appear, verify subcategory selection works inline
  </verify>
  <done>AG Grid uses multiRow selection with header checkbox. Inline dropdown shows full "Category / Subcategory" labels. All rows including Unknown are editable. Stable row IDs configured.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve filter/page state and fix subcategory cascading in review page</name>
  <files>
    src/store_predict/ui/pages/review.py
    src/store_predict/ui/components/workload_dialog.py
  </files>
  <action>
    In review.py:

    1. Add filter/page preservation to _handle_cell_change:
       BEFORE grid.update(), capture state:
       - Use grid.run_grid_method("getFilterModel") to get current filters (returns awaitable)
       - Use grid.run_grid_method("paginationGetCurrentPage") to get current page
       Since _handle_cell_change is sync, convert it to async and use await.
       AFTER grid.update(), restore state:
       - grid.run_grid_method("setFilterModel", filter_model) -- do NOT await this (causes JS timeout per research)
       - grid.run_grid_method("paginationGoToPage", page_number)

    2. Add same filter/page preservation to _handle_row_click (already async):
       - Same pattern: capture before, restore after grid.update()

    3. Fix _handle_cell_change to parse the new "Category / Subcategory" label:
       - The newValue from cellValueChanged will now be "Category / Subcategory" format
       - Split on " / " to extract category and subcategory
       - Look up DRR from drr_table.get_ratio(category, subcategory)
       - Update both workload_category and workload_subcategory in row_data

    4. Prepare subcategory_labels list for create_vm_table:
       - Build from workload_options: [f"{opt['category']} / {opt['subcategory']}" for opt in workload_options]
       - Pass as subcategory_labels parameter to create_vm_table

    5. Update create_vm_table call to pass the new parameter.

    In workload_dialog.py:

    6. Update WorkloadDialog to show category/subcategory grouping:
       - The existing options already use "Category / Subcategory" as labels
       - No structural change needed, just verify the current approach works with the new inline labels

    CRITICAL: Do NOT await setFilterModel -- it causes a JavaScript timeout. Just call it fire-and-forget with grid.run_grid_method().
  </action>
  <verify>
    rtk ruff check src/store_predict/ui/pages/review.py src/store_predict/ui/components/workload_dialog.py
    Manual: run app, set a filter, change a workload, verify filter remains active and page stays
  </verify>
  <done>Filters and page position survive workload edits. Subcategory is selectable via inline dropdown (full "Category / Subcategory" labels). Cell change handler parses the combined label correctly.</done>
</task>

</tasks>

<verification>
- rtk ruff check src/store_predict/ui/
- rtk mypy src/store_predict/ui/
- Manual verification: multi-select checkboxes, filter persistence, page persistence, subcategory selection
</verification>

<success_criteria>
- Multi-row selection with header checkbox works
- Filters preserved after any workload modification
- Page position preserved after any workload modification
- Inline dropdown shows "Category / Subcategory" labels
- Unknown (Reducible) rows are fully editable
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-03-SUMMARY.md`
</output>
