---
phase: 07-ui-bug-fixes-and-report-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store_predict/pipeline/parsers/columns.py
  - src/store_predict/pipeline/parsers/liveoptics.py
  - src/store_predict/pipeline/parsers/rvtools.py
  - src/store_predict/config.py
autonomous: true
requirements: [FR-1.1, FR-1.2]

must_haves:
  truths:
    - "LiveOptics xlsx parser extracts VM Performance sheet data (IOPS, throughput, latency)"
    - "Performance columns are NaN when data is unavailable (RVTools, LiveOptics CSV)"
    - "RVTools parser extracts Annotation column as vm_description"
    - "LiveOptics parser extracts Description/Notes if available"
    - "8K equivalent IOPS is computed per VM from IOPS + throughput"
    - "Company prefix patterns are configurable in config.py"
  artifacts:
    - path: "src/store_predict/pipeline/parsers/columns.py"
      provides: "Extended CANONICAL_COLUMNS with performance + description columns"
      contains: "peak_iops"
    - path: "src/store_predict/pipeline/parsers/liveoptics.py"
      provides: "parse_liveoptics_performance function, performance join"
      contains: "parse_liveoptics_performance"
    - path: "src/store_predict/pipeline/parsers/rvtools.py"
      provides: "Annotation column extraction as vm_description"
      contains: "vm_description"
    - path: "src/store_predict/config.py"
      provides: "COMPANY_PREFIX_PATTERNS list"
      contains: "COMPANY_PREFIX_PATTERNS"
  key_links:
    - from: "src/store_predict/pipeline/parsers/liveoptics.py"
      to: "src/store_predict/pipeline/parsers/columns.py"
      via: "PERFORMANCE_ALIASES, CANONICAL_COLUMNS import"
      pattern: "PERFORMANCE_ALIASES"
    - from: "src/store_predict/pipeline/parsers/liveoptics.py"
      to: "VM Performance sheet"
      via: "pd.read_excel with sheet_name='VM Performance'"
      pattern: "VM Performance"
---

<objective>
Extend the ingestion pipeline to extract performance data from LiveOptics, description/annotation fields from both formats, and add company prefix configuration.

Purpose: Foundation layer that all other Phase 7 plans depend on -- performance columns, description fields, and prefix config must exist before classification improvements, UI display, and report enhancements can use them.

Output: Extended canonical DataFrame schema with performance + description columns; LiveOptics performance sheet parser; config for company prefixes.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store_predict/pipeline/parsers/columns.py
@src/store_predict/pipeline/parsers/liveoptics.py
@src/store_predict/pipeline/parsers/rvtools.py
@src/store_predict/config.py
@.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend canonical schema with performance and description columns</name>
  <files>
    src/store_predict/pipeline/parsers/columns.py
    src/store_predict/config.py
  </files>
  <action>
    In columns.py:
    1. Add to CANONICAL_COLUMNS list (after "source_format"):
       - "vm_description" (string, from Annotation/Description/Notes)
       - "peak_iops" (float, NaN default)
       - "avg_iops" (float, NaN default)
       - "peak_throughput_mbs" (float, NaN default -- KB/s from source converted to MB/s)
       - "avg_throughput_mbs" (float, NaN default)
       - "peak_latency_ms" (float, NaN default)
       - "avg_read_latency_ms" (float, NaN default)
       - "avg_write_latency_ms" (float, NaN default)
       - "iops_8k_equivalent" (float, NaN default)

    2. Add RVTOOLS_ALIASES entry:
       - "vm_description": ["Annotation", "Notes"]

    3. Add LIVEOPTICS_ALIASES entries:
       - "vm_description": ["Description", "Notes", "Annotation"]

    4. Add new LIVEOPTICS_PERFORMANCE_ALIASES dict for VM Performance sheet columns:
       - "vm_name": ["VM Name"]
       - "peak_iops": ["Peak IOPS", "Peak Total IOPS"]
       - "avg_iops": ["Average IOPS", "Average Total IOPS"]
       - "peak_throughput_kbs": ["Max KB/sec", "Peak KB/sec", "Max Total KB/sec"]
       - "avg_throughput_kbs": ["Average KB/sec", "Average Total KB/sec"]
       - "peak_latency_ms": ["Max Latency(ms)", "Peak Latency(ms)", "Max Latency (ms)"]
       - "avg_read_latency_ms": ["Average Read Latency(ms)", "Average Read Latency (ms)"]
       - "avg_write_latency_ms": ["Average Write Latency(ms)", "Average Write Latency (ms)"]

    5. Add REQUIRED_LIVEOPTICS_PERFORMANCE_COLUMNS = {"vm_name", "peak_iops"} (minimal -- only vm_name and one metric required)

    In config.py:
    6. Add COMPANY_PREFIX_PATTERNS: list[str] = [] (empty by default, user configures). Add docstring explaining format: list of regex patterns anchored to start, e.g. [r"^ACME[-_]", r"^CORP[-_]"]. Also add PERFORMANCE_COLUMNS list constant for easy reference: ["peak_iops", "avg_iops", "peak_throughput_mbs", "avg_throughput_mbs", "peak_latency_ms", "avg_read_latency_ms", "avg_write_latency_ms", "iops_8k_equivalent"].
  </action>
  <verify>
    rtk pytest tests/ -x -q (existing tests must still pass -- canonical columns change must not break existing parsers)
    rtk ruff check src/store_predict/pipeline/parsers/columns.py src/store_predict/config.py
  </verify>
  <done>CANONICAL_COLUMNS includes all 9 new columns. Aliases defined for both formats. Config has COMPANY_PREFIX_PATTERNS and PERFORMANCE_COLUMNS. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend parsers to extract performance data and description fields</name>
  <files>
    src/store_predict/pipeline/parsers/liveoptics.py
    src/store_predict/pipeline/parsers/rvtools.py
  </files>
  <action>
    In liveoptics.py:
    1. Add new function parse_liveoptics_performance(path: Path) -> pd.DataFrame that:
       - Reads "VM Performance" sheet with pd.read_excel(path, sheet_name="VM Performance", engine="openpyxl")
       - Strips column whitespace
       - Uses resolve_columns with LIVEOPTICS_PERFORMANCE_ALIASES and REQUIRED_LIVEOPTICS_PERFORMANCE_COLUMNS
       - Returns DataFrame with columns: vm_name, peak_iops, avg_iops, peak_throughput_kbs, avg_throughput_kbs, peak_latency_ms, avg_read_latency_ms, avg_write_latency_ms
       - On KeyError/ValueError (sheet missing), returns empty DataFrame with those columns

    2. Modify _build_liveoptics_df to:
       - Extract vm_description from col_map (optional, default "")
       - Set all performance columns to float("nan") by default (NaN)
       - Set iops_8k_equivalent to float("nan")
       - Return the extended CANONICAL_COLUMNS

    3. Modify parse_liveoptics_xlsx to:
       - After building canonical df, call parse_liveoptics_performance(path)
       - If performance df is non-empty, join on vm_name (df.merge with left join, strip whitespace on join key)
       - Convert peak_throughput_kbs / avg_throughput_kbs from KB/s to MB/s (divide by 1024), store as peak_throughput_mbs / avg_throughput_mbs
       - Compute iops_8k_equivalent = avg_iops + (avg_throughput_kbs / 8.0) per research formula
       - Fill NaN for any missing performance values

    4. parse_liveoptics_csv: No performance data available in CSV format, just ensure the new columns exist with NaN defaults (already handled by _build_liveoptics_df).

    In rvtools.py:
    5. Modify parse_rvtools to:
       - Extract vm_description from col_map["vm_description"] if resolved (Annotation column), default ""
       - Add all performance columns as float("nan")
       - Add iops_8k_equivalent as float("nan")
       - Return extended CANONICAL_COLUMNS

    IMPORTANT: Use import for the new aliases/constants from columns.py. The join on VM Name must use .str.strip() on both sides to handle whitespace. All performance columns must be pd.to_numeric with errors="coerce".
  </action>
  <verify>
    rtk pytest tests/ -x -q (all existing tests pass with extended schema)
    python -c "from store_predict.pipeline.parsers.liveoptics import parse_liveoptics_xlsx; import pandas as pd; df = parse_liveoptics_xlsx(Path('samples/live-optics.xlsx')); print(df.columns.tolist()); print(df[['peak_iops','avg_iops','iops_8k_equivalent']].describe())"
    rtk ruff check src/store_predict/pipeline/parsers/
  </verify>
  <done>LiveOptics xlsx parser extracts VM Performance sheet and joins with VMs data. RVTools parser extracts Annotation as vm_description. All parsers produce extended canonical schema. Performance columns are NaN for RVTools and LiveOptics CSV. Existing tests pass.</done>
</task>

</tasks>

<verification>
- All existing tests pass (rtk pytest tests/ -x -q)
- LiveOptics xlsx output has performance columns populated from sample file
- RVTools output has vm_description populated from Annotation column
- LiveOptics CSV output has performance columns as NaN (graceful fallback)
- No ruff or mypy errors in modified files
</verification>

<success_criteria>
- Extended canonical DataFrame schema with 9 new columns
- LiveOptics performance data extracted and joined
- 8K equivalent IOPS computed per VM
- Description/Annotation fields extracted from both formats
- Graceful NaN fallback when performance data unavailable
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-01-SUMMARY.md`
</output>
