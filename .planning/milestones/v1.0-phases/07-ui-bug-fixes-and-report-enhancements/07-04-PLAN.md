---
phase: 07-ui-bug-fixes-and-report-enhancements
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-03]
files_modified:
  - src/store_predict/pipeline/calculation.py
  - src/store_predict/services/pdf_report.py
  - src/store_predict/ui/components/vm_table.py
  - src/store_predict/ui/pages/review.py
autonomous: true
requirements: [FR-5.1, FR-5.2, FR-5.3, FR-5.4, FR-6.1, FR-6.2, FR-6.3, FR-6.4, FR-6.5]

must_haves:
  truths:
    - "PDF report includes total number of VMs"
    - "PDF report includes average VM size (provisioned)"
    - "PDF report includes largest VM details (name + provisioned size)"
    - "PDF report includes performance summary when data available (total IOPS, peak throughput)"
    - "Review table displays vm_description column"
    - "Review table displays performance columns (peak_iops, iops_8k_equivalent) when data available"
    - "Performance columns are hidden when all values are NaN (RVTools data)"
  artifacts:
    - path: "src/store_predict/pipeline/calculation.py"
      provides: "Extended CalculationSummary with VM stats and performance totals"
      contains: "avg_vm_size_mib"
    - path: "src/store_predict/services/pdf_report.py"
      provides: "VM statistics section and performance section in PDF"
      contains: "VM Statistics"
    - path: "src/store_predict/ui/components/vm_table.py"
      provides: "Description and performance columns in AG Grid"
      contains: "vm_description"
  key_links:
    - from: "src/store_predict/services/pdf_report.py"
      to: "src/store_predict/pipeline/calculation.py"
      via: "CalculationSummary dataclass fields"
      pattern: "avg_vm_size_mib"
    - from: "src/store_predict/ui/components/vm_table.py"
      to: "row_data"
      via: "Column defs reference performance fields from row data"
      pattern: "peak_iops"
---

<objective>
Add VM statistics and performance data to the PDF report and review table. Enrich the review table with description and performance columns.

Purpose: Pre-sales engineers need VM count, average size, largest VM, and IOPS metrics in both the review UI and the PDF deliverable. Performance data is critical for PowerStore sizing.

Output: Enhanced PDF with VM stats + performance section; review table with description and performance columns.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store_predict/pipeline/calculation.py
@src/store_predict/services/pdf_report.py
@src/store_predict/ui/components/vm_table.py
@src/store_predict/ui/pages/review.py
@.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-01-SUMMARY.md
@.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend calculation summary with VM statistics and performance totals</name>
  <files>
    src/store_predict/pipeline/calculation.py
  </files>
  <action>
    1. Add new fields to CalculationSummary dataclass:
       - avg_vm_size_mib: float  (average provisioned_mib across all VMs)
       - largest_vm_name: str  (name of VM with highest provisioned_mib)
       - largest_vm_provisioned_mib: float  (provisioned MiB of largest VM)
       - total_peak_iops: float  (sum of peak_iops across all VMs, 0.0 if no perf data)
       - total_avg_iops: float  (sum of avg_iops across all VMs)
       - peak_throughput_mbs: float  (max of peak_throughput_mbs across all VMs)
       - total_iops_8k_equivalent: float  (sum of iops_8k_equivalent across all VMs)
       - has_performance_data: bool  (True if any VM has non-NaN performance values)

    2. Update VMCalculation dataclass to include:
       - peak_iops: float (default 0.0)
       - avg_iops: float (default 0.0)
       - peak_throughput_mbs: float (default 0.0)
       - iops_8k_equivalent: float (default 0.0)

    3. Update calculate() function:
       - Extract performance fields from row_data (use float(row.get("peak_iops", 0)) with math.isnan check, default 0.0 for NaN)
       - Import math for isnan checks
       - Compute avg_vm_size_mib = total_provisioned / total_vms (or 0.0 if no VMs)
       - Find largest VM by provisioned_mib
       - Sum performance metrics, tracking has_performance_data (True if any peak_iops > 0)
       - Find peak throughput as max across VMs
       - Pass all new fields to CalculationSummary constructor
  </action>
  <verify>
    rtk pytest tests/ -x -q
    rtk ruff check src/store_predict/pipeline/calculation.py
  </verify>
  <done>CalculationSummary includes avg VM size, largest VM details, performance totals, and has_performance_data flag. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add VM stats and performance to PDF report and review table</name>
  <files>
    src/store_predict/services/pdf_report.py
    src/store_predict/ui/components/vm_table.py
    src/store_predict/ui/pages/review.py
  </files>
  <action>
    In pdf_report.py:
    1. Add "VM Statistics" section after the existing Summary section:
       - Total VMs: (already in summary, but repeat for clarity)
       - Average VM Size: format_storage(summary.avg_vm_size_mib)
       - Largest VM: f"{summary.largest_vm_name} ({format_storage(summary.largest_vm_provisioned_mib)})"

    2. Add "Performance Summary" section (only if summary.has_performance_data is True):
       - Total Peak IOPS: f"{summary.total_peak_iops:,.0f}"
       - Total Average IOPS: f"{summary.total_avg_iops:,.0f}"
       - Peak Throughput: f"{summary.peak_throughput_mbs:,.1f} MB/s"
       - Total 8K Equivalent IOPS: f"{summary.total_iops_8k_equivalent:,.0f}"

    3. Place these sections BEFORE the workload breakdown table.

    In vm_table.py:
    4. Add vm_description column definition after os_name column:
       {"field": "vm_description", "headerName": "Description", "sortable": True, "filter": "agTextColumnFilter", "floatingFilter": True, "minWidth": 150}

    5. Add parameter has_performance_data: bool = False to create_vm_table.
       When True, add performance columns after the existing columns (before classification_confidence):
       - {"field": "peak_iops", "headerName": "Peak IOPS", "sortable": True, "filter": "agNumberColumnFilter", "valueFormatter": "value ? Math.round(value).toLocaleString() : ''"}
       - {"field": "iops_8k_equivalent", "headerName": "8K Eq. IOPS", "sortable": True, "filter": "agNumberColumnFilter", "valueFormatter": "value ? Math.round(value).toLocaleString() : ''"}
       - {"field": "peak_throughput_mbs", "headerName": "Peak MB/s", "sortable": True, "filter": "agNumberColumnFilter", "valueFormatter": "value ? value.toFixed(1) : ''"}

    In review.py:
    6. Detect has_performance_data: check if any row in row_data has a non-NaN peak_iops value.
       Use: has_perf = any(isinstance(r.get("peak_iops"), (int, float)) and r.get("peak_iops") and not (isinstance(r.get("peak_iops"), float) and math.isnan(r.get("peak_iops"))) for r in row_data)
       Simpler approach: import math; has_perf = any(r.get("peak_iops") is not None and not (isinstance(r.get("peak_iops"), float) and math.isnan(r["peak_iops"])) and float(r.get("peak_iops", 0)) > 0 for r in row_data)
    7. Pass has_performance_data=has_perf to create_vm_table.
  </action>
  <verify>
    rtk pytest tests/ -x -q
    rtk ruff check src/store_predict/services/pdf_report.py src/store_predict/ui/components/vm_table.py src/store_predict/ui/pages/review.py
  </verify>
  <done>PDF report includes VM statistics section (avg size, largest VM) and conditional performance summary. Review table shows description column and conditional performance columns. Performance columns hidden when data unavailable.</done>
</task>

</tasks>

<verification>
- rtk pytest tests/ -x -q (all tests pass)
- rtk ruff check src/store_predict/
- PDF generation with LiveOptics data includes performance section
- PDF generation with RVTools data omits performance section gracefully
- Review table shows description for both formats
</verification>

<success_criteria>
- PDF includes VM count, average VM size, largest VM name+size
- PDF includes performance summary (IOPS, throughput, 8K equivalent) when LiveOptics data available
- PDF gracefully omits performance section for RVTools/CSV data
- Review table shows description column
- Review table shows performance columns only when data exists
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-04-SUMMARY.md`
</output>
