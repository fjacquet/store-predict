---
phase: 05-calculation-pdf-report
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/store_predict/ui/pages/report.py
  - src/store_predict/ui/layout.py
  - src/store_predict/ui/pages/review.py
  - src/store_predict/main.py
autonomous: true
requirements:
  - FR-5.4
  - FR-6.5

must_haves:
  truths:
    - "Report page displays summary cards with total VMs, provisioned, in_use, required, weighted avg DRR"
    - "Report page displays workload breakdown table with Category, VM count, Provisioned, DRR, Required"
    - "Download PDF button triggers browser download of generated PDF"
    - "Navigation from Review page to Report page works"
    - "Report page accessible at /report route"
  artifacts:
    - path: "src/store_predict/ui/pages/report.py"
      provides: "Report page with summary cards, breakdown table, and download button"
      min_lines: 60
  key_links:
    - from: "src/store_predict/ui/pages/report.py"
      to: "src/store_predict/pipeline/calculation.py"
      via: "Calls calculate() on session VM data"
      pattern: "from store_predict.pipeline.calculation import"
    - from: "src/store_predict/ui/pages/report.py"
      to: "src/store_predict/services/pdf_report.py"
      via: "Calls generate_report_pdf() for download"
      pattern: "from store_predict.services.pdf_report import"
    - from: "src/store_predict/ui/pages/report.py"
      to: "src/store_predict/ui/state.py"
      via: "Reads vm_data and project_name from app.storage.tab"
      pattern: "app.storage.tab"
    - from: "src/store_predict/ui/pages/review.py"
      to: "/report"
      via: "Generate Report navigation button"
      pattern: "ui.navigate.to.*report"
---

<objective>
Build the report page with summary cards, workload breakdown table, and PDF download button. Wire navigation from Review -> Report and add /report to the nav bar.

Purpose: Completes the end-to-end user flow: upload -> classify -> review -> calculate -> download PDF.
Output: Report page at /report with calculation results display and PDF download trigger.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calculation-pdf-report/05-RESEARCH.md
@.planning/phases/05-calculation-pdf-report/05-01-SUMMARY.md
@.planning/phases/05-calculation-pdf-report/05-02-SUMMARY.md
@src/store_predict/ui/layout.py
@src/store_predict/ui/state.py
@src/store_predict/ui/pages/review.py
@src/store_predict/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create report page with calculation display and PDF download</name>
  <files>
    src/store_predict/ui/pages/report.py
  </files>
  <action>
Create `src/store_predict/ui/pages/report.py` following the existing page patterns (see review.py, upload.py).

Implementation:

1. **Page registration** — `@ui.page('/report')` decorator. Use the shared layout context manager.

2. **Session data loading** — `await ui.context.client.connected()` then read from `app.storage.tab`:
   - `vm_data`: list of row dicts (classified VMs)
   - `project_name`: string (from upload page)
   - If no data: show "No data available. Please upload a file first." with link to /upload.

3. **Run calculation** — Call `calculate(vm_data)` from `pipeline.calculation` to get `CalculationSummary`.

4. **Summary cards section** — Use a card grid (Tailwind `grid grid-cols-2 md:grid-cols-4 gap-4`) showing:
   - Total VMs (summary.total_vms)
   - Total Provisioned (format_storage(summary.total_provisioned_mib))
   - Total In Use (format_storage(summary.total_in_use_mib))
   - Weighted Avg DRR (f"{summary.weighted_avg_drr:.1f}x")
   - Required Capacity (format_storage(summary.total_required_mib))
   Each card: `ui.card()` with label and value, styled with Tailwind.

5. **Workload breakdown table** — Use NiceGUI `ui.table` (not AG Grid — simpler for read-only display):
   - Columns: Category, VMs, Provisioned (GiB), Avg DRR, Required (GiB)
   - Rows from summary.workload_groups
   - Format storage as GiB (mib / 1024, 1 decimal)
   - Format DRR as "X.Xx"

6. **Download button** — `ui.button('Download PDF Report', on_click=_on_download, icon='download')` styled `bg-blue-700 text-white`.
   - `_on_download` function calls `generate_report_pdf(summary, project_name)` then `ui.download(content=pdf_bytes, filename=f"StorePredict_{safe_name}_{date_str}.pdf", media_type='application/pdf')`
   - Use `sanitize_filename` from pdf_report service for safe filename
   - date_str = today's date in YYYY-MM-DD format

7. **Back navigation** — "Back to Review" button linking to /review.

Run: `rtk ruff check src/store_predict/ui/pages/report.py` and `rtk mypy src/store_predict/ui/pages/report.py` — clean.
  </action>
  <verify>rtk ruff check and rtk mypy clean; page imports resolve without errors</verify>
  <done>Report page at /report displays summary cards, workload breakdown table, and PDF download button; reads session data and runs calculation</done>
</task>

<task type="auto">
  <name>Task 2: Wire navigation and register report page</name>
  <files>
    src/store_predict/ui/layout.py
    src/store_predict/ui/pages/review.py
    src/store_predict/main.py
  </files>
  <action>
Wire the report page into the application:

1. **Register page module** — In `src/store_predict/main.py`, add `from store_predict.ui.pages import report  # noqa: F401` alongside existing page imports (same pattern as upload, review).

2. **Add nav link** — In `src/store_predict/ui/layout.py`, add "Report" link to the navigation bar:

   ```python
   ui.link("Report", "/report").classes("text-white no-underline hover:underline")
   ```

   Place after the existing "Review" link.

3. **Add Review -> Report button** — In `src/store_predict/ui/pages/review.py`, add a "Generate Report" button at the bottom of the page (after the AG Grid and summary stats):

   ```python
   ui.button("Generate Report", on_click=lambda: ui.navigate.to("/report")) \
       .classes("bg-blue-700 text-white")
   ```

Run: `rtk ruff check src/store_predict/ui/layout.py src/store_predict/ui/pages/review.py src/store_predict/main.py` — clean.
Then: `rtk pytest` — all tests still pass (no regressions).
  </action>
  <verify>rtk ruff check clean on all modified files; rtk pytest — all tests pass</verify>
  <done>Report page registered at /report; nav bar includes Report link; Review page has "Generate Report" button; all existing tests pass</done>
</task>

</tasks>

<verification>
- `rtk ruff check .` — clean
- `rtk mypy src/` — clean
- `rtk pytest` — all tests pass (82+ existing + new calculation + PDF tests)
- Report page accessible at /report
- Navigation: Upload → Review → Report flow complete
- PDF download button present on report page
</verification>

<success_criteria>

- Report page shows summary cards with calculated totals
- Report page shows workload breakdown table
- Download PDF button generates and downloads a branded PDF
- Review page has "Generate Report" button navigating to /report
- Nav bar includes Report link
- All tests pass with no regressions
- End-to-end flow: upload → classify → review → calculate → download PDF
</success_criteria>

<output>
After completion, create `.planning/phases/05-calculation-pdf-report/05-03-SUMMARY.md`
</output>
