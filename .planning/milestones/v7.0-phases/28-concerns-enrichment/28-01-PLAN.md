---
phase: 28-concerns-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store_predict/pipeline/health_checks.py
  - src/store_predict/ui/pages/concerns.py
  - src/store_predict/i18n/locales/en.yaml
  - src/store_predict/i18n/locales/fr.yaml
  - tests/test_health_checks.py
autonomous: true
requirements:
  - CONC-01

must_haves:
  truths:
    - "Every HealthFinding dataclass instance carries a non-empty remediation string"
    - "All 13 check functions in health_checks.py populate the remediation field with an actionable English sentence"
    - "The /concerns page renders remediation text below the detail text in muted italic style"
    - "Tests confirm the remediation field is populated for each check type"
  artifacts:
    - path: "src/store_predict/pipeline/health_checks.py"
      provides: "HealthFinding dataclass with remediation field, all check functions updated"
      contains: "remediation: str"
    - path: "src/store_predict/ui/pages/concerns.py"
      provides: "UI card rendering remediation hint in italic muted style"
      contains: "finding.remediation"
    - path: "tests/test_health_checks.py"
      provides: "Tests verifying remediation field presence for representative checks"
      contains: "remediation"
  key_links:
    - from: "src/store_predict/pipeline/health_checks.py"
      to: "src/store_predict/ui/pages/concerns.py"
      via: "HealthFinding.remediation field read in _render_finding_card"
      pattern: "finding\\.remediation"
---

<objective>
Add a `remediation` field to `HealthFinding` and populate each of the 13 check functions with a concise actionable hint. Display the hint on the /concerns page below the detail text in a muted italic style.

Purpose: Pre-sales engineers need to know what to tell the customer to do about each health finding — a hint like "Upgrade VMware Tools on these VMs before migration" is more valuable than a bare description.
Output: Extended dataclass, 13 populated hints, UI update, i18n keys for UI labels, tests.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store_predict/pipeline/health_checks.py
@src/store_predict/ui/pages/concerns.py
@src/store_predict/i18n/locales/en.yaml
@src/store_predict/i18n/locales/fr.yaml
@tests/test_health_checks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend HealthFinding dataclass and populate remediation hints in all check functions</name>
  <files>src/store_predict/pipeline/health_checks.py</files>
  <action>
    1. Add `remediation: str = ""` field to the `HealthFinding` frozen dataclass (after the `cluster` field). The field has a default so existing construction sites with no remediation remain valid.

    2. Populate the `remediation` field in every check function. Use hardcoded English strings (pre-sales tool — English hints are acceptable per planning decision in STATE.md). One actionable sentence per check:

    - `_check_missing_os`: `"Export the RVTools file after ensuring VMware Tools is running on affected VMs — Tools populates the OS field."`
    - `_check_zero_provisioned`: `"Re-export the RVTools file; zero provisioned storage usually indicates a snapshot-only or powered-off VM missing disk data."`
    - `_check_missing_cpu`: `"Re-export after powering on these VMs — CPU data is absent for powered-off VMs in some RVTools versions."`
    - `_check_missing_ram`: `"Re-export after powering on these VMs — RAM data is absent for powered-off VMs in some RVTools versions."`
    - `_check_high_powered_off_ratio`: `"Confirm with the customer whether these are truly decommissioned VMs or if the export was taken during a maintenance window."`
    - `_check_high_unknown_ratio`: `"Review the VM Review page and manually classify these VMs, or add pattern rules to the classifier for your customer's naming convention."`
    - `_check_large_unknown_vms`: `"Classify these large VMs on the VM Review page before generating the final report — their DRR has a major impact on total required capacity."`
    - `_check_iops_budget_exceeded`: `"These VMs will require a dedicated datastore or a high-performance NVMe-tier datastore; discuss PowerStore T-series options with the customer."`
    - `_check_no_cluster`: `"These standalone hosts cannot use vMotion; confirm whether they are test/dev hosts or production hosts that should be clustered."`
    - `_check_hw_version_per_cluster` (very_old): `"Schedule an immediate ESXi upgrade to 7.0 U3 or later for this cluster — vHW below 14 is incompatible with modern vSphere features."`
    - `_check_hw_version_per_cluster` (old): `"Plan an ESXi upgrade to 7.0 U3 or later for this cluster — vHW 14–16 cannot use the latest VM features and paravirtual drivers."`
    - `_check_small_cluster_ha`: `"Add at least one more ESXi host to this cluster to achieve N+1 HA; a 2-host cluster loses 50% capacity on a single host failure."`
    - `_check_tools_status` (not_installed): `"Install VMware Tools on these VMs before migration — without Tools, quiesced snapshots and guest OS commands are unavailable."`
    - `_check_tools_status` (not_running): `"Start the VMware Tools service on these VMs — Tools is installed but inactive, which disables quiesced snapshots and in-guest commands."`

    3. Update `__all__` if needed (no change required — existing exports are correct).
  </action>
  <verify>rtk pytest tests/test_health_checks.py -x</verify>
  <done>All existing health check tests pass. `HealthFinding` dataclass has `remediation` field. Each check function sets a non-empty remediation string.</done>
</task>

<task type="auto">
  <name>Task 2: Display remediation hint in /concerns card UI and add i18n label key</name>
  <files>
    src/store_predict/ui/pages/concerns.py
    src/store_predict/i18n/locales/en.yaml
    src/store_predict/i18n/locales/fr.yaml
    tests/test_health_checks.py
  </files>
  <action>
    1. In `src/store_predict/ui/pages/concerns.py`, update `_render_finding_card` to display the remediation hint when it is non-empty. Add it below the existing `ui.label(t(finding.detail, ...))` line:

    ```python
    if finding.remediation:
        ui.label(finding.remediation).classes("text-sm text-gray-500 italic mt-1")
    ```

    No i18n key needed for the hint text itself (it is hardcoded English per planning decision). However, add an optional label prefix "Remediation:" as an i18n key for the UI — render it as a small bold label before the hint text if desired. Keep it simple: just the italic hint line, no prefix label needed.

    2. Add the following i18n keys to `src/store_predict/i18n/locales/en.yaml` under the `concerns:` section:
    ```yaml
    export_pdf: "Export PDF"
    export_csv: "Export CSV"
    export_pdf_filename: "concerns-report.pdf"
    export_csv_filename: "concerns-report.csv"
    ```
    (These are needed in Plan 02 but added here to keep i18n in one task.)

    3. Add the same keys to `src/store_predict/i18n/locales/fr.yaml`:
    ```yaml
    export_pdf: "Exporter PDF"
    export_csv: "Exporter CSV"
    export_pdf_filename: "rapport-preoccupations.pdf"
    export_csv_filename: "rapport-preoccupations.csv"
    ```

    4. In `tests/test_health_checks.py`, add tests verifying the `remediation` field:
    - Add a test `test_remediation_field_present_for_missing_os` that creates a DataFrame triggering `data_quality.missing_os` and asserts `finding.remediation != ""`.
    - Add a test `test_remediation_field_present_for_tools_not_installed` that triggers `best_practice.tools_not_installed` and asserts `finding.remediation != ""`.
    - Add a test `test_remediation_field_default_empty` that constructs a `HealthFinding` without `remediation` and asserts `finding.remediation == ""`.

    Use the existing `_make_active_df` helper from the test file (do NOT use unittest.mock).
  </action>
  <verify>rtk pytest tests/test_health_checks.py -x && rtk ruff check src/store_predict/ui/pages/concerns.py src/store_predict/pipeline/health_checks.py</verify>
  <done>Concerns page renders remediation hint in italic gray below the detail text. Three new tests pass. i18n keys for export buttons exist in both en.yaml and fr.yaml. Ruff lint passes.</done>
</task>

</tasks>

<verification>
rtk pytest tests/test_health_checks.py -v
rtk mypy src/store_predict/pipeline/health_checks.py src/store_predict/ui/pages/concerns.py
</verification>

<success_criteria>
- `HealthFinding` has a `remediation: str = ""` field
- All 13 check functions (14 finding types) set a non-empty remediation string
- `/concerns` page shows the hint in italic muted style below detail text
- 3 new tests verifying remediation field pass
- mypy reports no new type errors
- All previously passing health check tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/28-concerns-enrichment/28-01-SUMMARY.md`
</output>
