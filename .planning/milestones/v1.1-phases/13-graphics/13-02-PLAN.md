---
phase: 13-graphics
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/store_predict/ui/pages/report.py
autonomous: true
requirements:
  - GFX-01

must_haves:
  truths:
    - "Report page renders a 'Data Visualizations' heading section below the existing workload breakdown table"
    - "Sankey chart appears full-width at the top of the charts section"
    - "Pie chart and DRR bar chart appear side-by-side in a two-column grid"
    - "Before/after bar chart appears full-width at the bottom spanning both columns"
    - "Charts are interactive: hover tooltips work on all four charts"
    - "Charts section only renders when summary.workload_groups is non-empty"
    - "Charts use Dell blue palette (not ECharts default rainbow)"
    - "All chart section labels go through t() from store_predict.i18n"
  artifacts:
    - path: "src/store_predict/ui/pages/report.py"
      provides: "ECharts charts section in report page"
      contains: "_build_charts_section"
  key_links:
    - from: "src/store_predict/ui/pages/report.py"
      to: "src/store_predict/services/charts.py"
      via: "import echart_sankey_options etc."
      pattern: "from store_predict.services.charts import"
    - from: "src/store_predict/ui/pages/report.py"
      to: "store_predict.i18n"
      via: "t('report.charts_heading')"
      pattern: "t\\(\"report\\.charts_heading\"\\)"
---

<objective>
Wire the four ECharts visualizations into the web UI report page, displaying them in a responsive layout below the existing workload breakdown table.

Purpose: Delivers GFX-01 — interactive charts for the web UI report page.
Output: report.py updated with a charts section using ui.echart.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-graphics/13-CONTEXT.md
@.planning/phases/13-graphics/13-RESEARCH.md
@.planning/phases/13-graphics/13-01-SUMMARY.md
@src/store_predict/ui/pages/report.py
@src/store_predict/services/charts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add charts section to report.py</name>
  <files>src/store_predict/ui/pages/report.py</files>
  <action>
    In `src/store_predict/ui/pages/report.py`:

    1. Add import at the top (with other service imports):
       ```python
       from store_predict.services.charts import (
           echart_before_after_options,
           echart_drr_bar_options,
           echart_pie_options,
           echart_sankey_options,
       )
       ```

    2. Add a private helper function `_build_charts_section(summary: CalculationSummary) -> None` after the existing helper functions (before `report_page`). The function:
       - Guards: if not summary.workload_groups, returns immediately (no charts for empty data).
       - Creates a section card or column with a heading label `t("report.charts_heading")` styled as "text-xl font-bold mb-4".
       - Full-width Sankey row:
         `with ui.row().classes("w-full"): ui.echart(echart_sankey_options(summary)).classes("w-full h-72")`
       - Two-column grid for pie + DRR bar:
         `with ui.grid(columns=2).classes("w-full gap-4"):`
           `ui.echart(echart_pie_options(summary)).classes("h-64")`
           `ui.echart(echart_drr_bar_options(summary)).classes("h-64")`
       - Full-width before/after bar (span both columns):
         `with ui.row().classes("w-full"):`
           `ui.echart(echart_before_after_options(summary)).classes("w-full h-64")`

    3. In `report_page()`, after the existing workload breakdown table section (and before the download buttons section if present, or at the end of the with-layout block), call `_build_charts_section(summary)`.

    Keep `TYPE_CHECKING` import for `CalculationSummary` if it is already used that way, or add it to the regular imports since it is now used at runtime in the function signature. Since `_build_charts_section` is called at runtime, `CalculationSummary` must be a runtime import (not under `TYPE_CHECKING`). Check the existing import style in report.py and follow it.
  </action>
  <verify>rtk python -c "from store_predict.ui.pages.report import report_page; print('OK')"</verify>
  <done>report.py imports cleanly; _build_charts_section defined; called in report_page after workload table.</done>
</task>

<task type="auto">
  <name>Task 2: Lint, type-check, and run tests</name>
  <files>src/store_predict/ui/pages/report.py</files>
  <action>
    Run the following quality checks in sequence:
    1. `rtk ruff check src/store_predict/ui/pages/report.py` — fix any lint errors.
    2. `rtk ruff format src/store_predict/ui/pages/report.py` — apply formatting.
    3. `rtk mypy src/store_predict/ui/pages/report.py` — fix any type errors (ensure CalculationSummary import is correct for runtime use).
    4. `rtk pytest tests/ -x -q` — run full test suite; fix any regressions.

    Common issues to watch for:
    - If CalculationSummary was previously only under TYPE_CHECKING in report.py, move it to a runtime import since _build_charts_section uses it at runtime.
    - If mypy complains about ui.echart not found, it is already suppressed by the `nicegui.*` override in pyproject.toml.
  </action>
  <verify>rtk pytest tests/ -x -q 2>&1 | tail -5</verify>
  <done>ruff passes clean, mypy passes, all existing tests pass with no regressions.</done>
</task>

</tasks>

<verification>
- `python -m store_predict.main` starts without import errors
- Browser: navigate to /report (with session data) — "Data Visualizations" heading visible
- Sankey chart renders full-width with Dell blue nodes
- Pie + DRR bar charts render side by side
- Before/after bar chart renders full-width below
- Hover tooltips work on all charts
- `rtk pytest tests/ -q` passes
</verification>

<success_criteria>
- report.py has _build_charts_section function called from report_page
- Four ui.echart calls in the charts section
- All labels use t() for i18n
- ruff, mypy, pytest all pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-graphics/13-02-SUMMARY.md`
</output>
