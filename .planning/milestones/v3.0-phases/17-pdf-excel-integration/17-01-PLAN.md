---
phase: 17-pdf-excel-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store_predict/services/pdf_report.py
  - src/store_predict/services/excel_report.py
  - src/store_predict/i18n/locales/en.yaml
  - src/store_predict/i18n/locales/fr.yaml
  - tests/test_pdf_report.py
  - tests/test_excel_report.py
autonomous: true
requirements:
  - REQ-012
  - REQ-013

must_haves:
  truths:
    - "PDF report contains a layout recommendations page after the charts section"
    - "Excel export contains a fourth sheet with layout recommendations"
    - "Layout content respects locale setting (FR/EN)"
    - "Empty summary (total_vms == 0) skips layout page/sheet gracefully"
  artifacts:
    - path: "src/store_predict/services/pdf_report.py"
      provides: "_build_layout_page() helper and _layout_metric_rows() shared builder"
      contains: "_build_layout_page"
    - path: "src/store_predict/services/excel_report.py"
      provides: "_write_layout_sheet() helper"
      contains: "_write_layout_sheet"
    - path: "src/store_predict/i18n/locales/en.yaml"
      provides: "pdf.layout_heading, pdf.layout_strategy_label, excel.sheet_layout keys"
      contains: "layout_heading"
    - path: "src/store_predict/i18n/locales/fr.yaml"
      provides: "French equivalents for layout PDF/Excel keys"
      contains: "layout_heading"
  key_links:
    - from: "src/store_predict/services/pdf_report.py"
      to: "pipeline.layout_engine.generate_all_proposals"
      via: "internal call with default PlacementConstraints"
      pattern: "generate_all_proposals"
    - from: "src/store_predict/services/excel_report.py"
      to: "pipeline.layout_engine.generate_all_proposals"
      via: "internal call with default PlacementConstraints"
      pattern: "generate_all_proposals"
    - from: "src/store_predict/services/excel_report.py"
      to: "src/store_predict/services/pdf_report.py"
      via: "import _layout_metric_rows"
      pattern: "_layout_metric_rows"
---

<objective>
Add layout recommendation output to both PDF and Excel reports.

Purpose: Complete the export pipeline so users can download sizing reports that include datastore layout strategy comparisons alongside existing DRR data.
Output: PDF page 3 with strategy comparison table; Excel sheet 4 with comparison summary + per-strategy datastore detail tables; updated tests.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-pdf-excel-integration/17-RESEARCH.md
@src/store_predict/services/pdf_report.py
@src/store_predict/services/excel_report.py
@src/store_predict/ui/pages/layout_page.py
@src/store_predict/pipeline/layout_models.py
@src/store_predict/pipeline/layout_engine.py
@src/store_predict/i18n/locales/en.yaml
@src/store_predict/i18n/locales/fr.yaml
@tests/test_pdf_report.py
@tests/test_excel_report.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys and layout page to PDF report</name>
  <files>
    src/store_predict/i18n/locales/en.yaml
    src/store_predict/i18n/locales/fr.yaml
    src/store_predict/services/pdf_report.py
    tests/test_pdf_report.py
  </files>
  <action>
**Step 1 — i18n keys.** Add 3 new keys to BOTH en.yaml and fr.yaml:
- `pdf.layout_heading`: EN `"Datastore Layout Recommendations"` / FR `"Recommandations de disposition des datastores"`
- `pdf.layout_strategy_label`: EN `"Strategy"` / FR `"Stratégie"`
- `excel.sheet_layout`: EN `"Layout Recommendations"` / FR `"Recommandations de disposition"`

Place under the existing `pdf:` and `excel:` namespaces respectively.

**Step 2 — Shared metric row builder.** In `pdf_report.py`, add a module-level helper `_layout_metric_rows()` that:
- Takes `proposals: list[LayoutProposal]` (import from `pipeline.layout_models`)
- Builds a dict lookup `{p.strategy_name: p for p in proposals}` to avoid index-order assumptions (pitfall 3 from research)
- Returns `list[tuple[str, str, str, str]]` — each tuple is `(metric_i18n_key, consolidation_val, performance_val, uniform_val)`
- Covers all 15 metrics matching `layout_page.py`'s `_build_comparison_table()`: ds_count, raw_capacity, usable_capacity, used_capacity, avg_utilization, min_utilization, max_utilization, avg_vm_density, max_vm_density, total_iops, max_iops_ds, iops_headroom, isolation_score, snapshot_rating, oversized_vms
- Add local `_fmt_tib(mib)` and `_fmt_pct(pct)` one-liner helpers (copy from layout_page.py, do NOT import from UI)
- Export `_layout_metric_rows` in `__all__` so `excel_report.py` can import it

**Step 3 — PDF layout page.** In `generate_report_pdf()`, after the charts section (after the before/after bar chart), add:
- Guard: `if summary.total_vms == 0` skip layout page entirely (no PageBreak, no table)
- Import `generate_all_proposals` from `pipeline.layout_engine` and `PlacementConstraints` from `pipeline.layout_models`
- Call `proposals = generate_all_proposals(summary)` with default constraints
- Append `PageBreak()` to story
- Append heading `Paragraph(t("pdf.layout_heading"), heading_style)`
- Append `Spacer(1, 12)`
- Build comparison table: header row `[t("layout_page.metric"), t("strategy.consolidation"), t("strategy.performance"), t("strategy.uniform")]`, then metric rows from `_layout_metric_rows(proposals)` — each row uses `t(f"metrics.{metric_key}")` for col 0
- Use `Table` with `colWidths=[160, 100, 100, 100]` (A4 usable ~460pt after margins)
- Apply `TableStyle` matching existing brand: `_BRAND_BLUE` header bg, white header text, `VeraBd` header font, `Vera` body font, 9pt, grey grid, RIGHT-aligned value columns, MIDDLE valign, 3pt padding
- Append table to story

**Step 4 — PDF test.** In `tests/test_pdf_report.py`, add a test class `TestPdfLayoutPage` with:
- `test_layout_page_locale_differs`: Generate PDF with EN and FR locales using a summary with `total_vms > 0`; assert `en_bytes != fr_bytes`
- `test_layout_page_skipped_when_empty`: Generate PDF with `total_vms == 0`; assert PDF is still valid bytes (starts with `b"%PDF-"`) but is smaller than one with layout data (or just check it does not error)
- Use the existing `_make_summary()` fixture pattern from the test file
  </action>
  <verify>
`rtk pytest tests/test_pdf_report.py -v` — all existing + new tests pass.
`rtk pytest tests/test_i18n.py -v` — locale key parity check passes.
  </verify>
  <done>
PDF report has a layout recommendations page with 4-column strategy comparison table. EN/FR locale output differs. Empty summary skips layout page without error. All i18n keys exist in both locales.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add layout sheet to Excel report and update tests</name>
  <files>
    src/store_predict/services/excel_report.py
    tests/test_excel_report.py
  </files>
  <action>
**Step 1 — _write_layout_sheet helper.** In `excel_report.py`, add a new function `_write_layout_sheet()` with signature:
```python
def _write_layout_sheet(
    wb: xlsxwriter.Workbook,
    summary: CalculationSummary,
    header_fmt: Format,
    bold_fmt: Format,
    number_fmt: Format,
) -> None:
```

Implementation:
- Guard: `if summary.total_vms == 0: return` — skip sheet entirely for empty data
- Import `generate_all_proposals` from `pipeline.layout_engine` and `PlacementConstraints` from `pipeline.layout_models`
- Call `proposals = generate_all_proposals(summary)` with default constraints
- Import `_layout_metric_rows` from `services.pdf_report` (shared helper from Task 1)
- Create worksheet: `ws = wb.add_worksheet(_i18n.t("excel.sheet_layout"))`
- **Comparison summary at top (row 0+):**
  - Header row: `_i18n.t("layout_page.metric")`, `_i18n.t("strategy.consolidation")`, `_i18n.t("strategy.performance")`, `_i18n.t("strategy.uniform")` — use `header_fmt`
  - Metric rows from `_layout_metric_rows(proposals)`: col 0 uses `_i18n.t(f"metrics.{metric_key}")` with `bold_fmt`, cols 1-3 write values
- **Blank separator row**
- **Per-strategy detail sub-tables:** For each proposal (use dict lookup `{p.strategy_name: p for p in proposals}` iterating over `["consolidation", "performance", "uniform"]`):
  - Strategy heading row: `_i18n.t(f"strategy.{strategy_name}")` with `bold_fmt`
  - Datastore sub-table header row: `_i18n.t("ds.name")`, `_i18n.t("ds.raw_cap")`, `_i18n.t("ds.used")`, `_i18n.t("ds.util")`, `_i18n.t("ds.vms")`, `_i18n.t("ds.iops")`, `_i18n.t("ds.workloads")` — use `header_fmt`
  - Datastore data rows: `ds.name`, `ds.raw_capacity_mib / (1024*1024)` (TiB, number_fmt), `ds.used_capacity_mib / (1024*1024)` (TiB, number_fmt), `ds.utilization_pct` (number_fmt), `ds.vm_count`, `ds.total_iops` (number_fmt), `", ".join(sorted(ds.workload_types))`
  - Blank separator between strategies
- Freeze panes at row 1, col 0: `ws.freeze_panes(1, 0)`
- Auto-fit columns: `ws.autofit()`

**Step 2 — Wire into generate_report_xlsx.** In `generate_report_xlsx()`, add the call to `_write_layout_sheet(wb, summary, header_fmt, bold_fmt, number_fmt)` BEFORE `wb.close()`, after the existing `_write_vm_detail_sheet()` call.

**Step 3 — Update existing test.** In `tests/test_excel_report.py`:
- Rename `TestExcelSheetCount::test_workbook_has_three_sheets` to `test_workbook_has_four_sheets`
- Update assertion: check for `xl/worksheets/sheet4.xml` in zipfile namelist
- Keep existing assertions for sheets 1-3

**Step 4 — Add layout sheet tests.** In `tests/test_excel_report.py`, add a test class `TestExcelLayoutSheet` with:
- `test_layout_sheet_exists`: Generate xlsx, open with zipfile, assert `xl/worksheets/sheet4.xml` in namelist
- `test_layout_sheet_locale_differs`: Generate EN and FR xlsx; assert bytes differ
- `test_layout_sheet_skipped_when_empty`: Generate xlsx with `total_vms == 0`; assert `xl/worksheets/sheet4.xml` NOT in namelist (only 3 sheets)

Use the existing `_make_summary()` and `_make_perf_summary()` helper patterns.
  </action>
  <verify>
`rtk pytest tests/test_excel_report.py -v` — all existing + new tests pass (renamed sheet count test passes with 4 sheets).
`rtk pytest tests/ -v` — full test suite passes with no regressions.
`rtk ruff check src/store_predict/services/excel_report.py` — no lint errors.
  </verify>
  <done>
Excel workbook has 4 sheets: Summary, Breakdown, VM Detail, Layout Recommendations. Layout sheet has comparison summary + 3 per-strategy datastore detail sub-tables. EN/FR locale output differs. Empty summary produces 3-sheet workbook (layout sheet skipped). All tests pass including updated sheet count test.
  </done>
</task>

</tasks>

<verification>
1. `rtk pytest tests/test_pdf_report.py tests/test_excel_report.py tests/test_i18n.py -v` — all tests pass
2. `rtk ruff check src/store_predict/services/` — no lint errors in modified services
3. `rtk mypy src/store_predict/services/pdf_report.py src/store_predict/services/excel_report.py` — no type errors
4. Manual spot check: generate a PDF and Excel from the app with sample data, verify layout page/sheet appears
</verification>

<success_criteria>
- PDF report page 3 shows layout strategy comparison table with 15 metrics across 3 strategies
- Excel workbook sheet 4 shows comparison summary + per-strategy datastore detail tables
- Both outputs respect FR/EN locale setting
- Empty summary (0 VMs) gracefully skips layout content in both formats
- All existing tests pass unchanged (except renamed sheet count test)
- New tests validate layout content presence, locale variation, and empty-data handling
</success_criteria>

<output>
After completion, create `.planning/phases/17-pdf-excel-integration/17-01-SUMMARY.md`
</output>
