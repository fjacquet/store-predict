---
phase: 15-default-iops-and-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - samples/IOPS.csv
  - src/store_predict/pipeline/layout_models.py
  - tests/test_layout_engine.py
autonomous: true
requirements:
  - REQ-014

must_haves:
  truths:
    - "IOPS defaults are loaded from samples/IOPS.csv at module import time"
    - "If IOPS.csv is missing, hardcoded fallback dict is used (tests still pass without CSV)"
    - "CSV uses semicolon delimiter matching DRR.csv convention"
    - "All 8 workload categories have IOPS values matching current DEFAULT_IOPS_BY_WORKLOAD"
  artifacts:
    - path: "samples/IOPS.csv"
      provides: "Semicolon-delimited IOPS reference table"
      contains: "Workload Category;IOPS Estimate"
    - path: "src/store_predict/pipeline/layout_models.py"
      provides: "_load_iops_from_csv() function and CSV-backed DEFAULT_IOPS_BY_WORKLOAD"
      contains: "_load_iops_from_csv"
    - path: "tests/test_layout_engine.py"
      provides: "Tests for CSV loading, fallback, and whitespace stripping"
      contains: "test_load_iops_from_csv"
  key_links:
    - from: "src/store_predict/pipeline/layout_models.py"
      to: "samples/IOPS.csv"
      via: "_load_iops_from_csv reads CSV at import time"
      pattern: "_load_iops_from_csv"
    - from: "src/store_predict/pipeline/layout_engine.py"
      to: "src/store_predict/pipeline/layout_models.py"
      via: "imports DEFAULT_IOPS_BY_WORKLOAD for _apply_default_iops()"
      pattern: "DEFAULT_IOPS_BY_WORKLOAD"
---

<objective>
Make IOPS defaults configurable via CSV file, following the established DRR.csv pattern.

Purpose: REQ-014 requires IOPS defaults to be "configurable via CSV or code constants." The hardcoded dict satisfies "code constants" but CSV allows pre-sales engineers to adjust IOPS estimates without code changes, matching how DRR.csv already works.

Output: samples/IOPS.csv, refactored layout_models.py with CSV loader, tests for loader.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-default-iops-and-docs/15-RESEARCH.md
@src/store_predict/pipeline/layout_models.py
@src/store_predict/pipeline/layout_engine.py
@tests/test_layout_engine.py
@samples/DRR.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IOPS.csv and refactor layout_models.py for CSV loading</name>
  <files>samples/IOPS.csv, src/store_predict/pipeline/layout_models.py</files>
  <action>
1. Create `samples/IOPS.csv` with semicolon delimiter (matching DRR.csv convention):
```
Workload Category;IOPS Estimate
Database/Microsoft SQL;500
Database/Oracle;800
Database/SAP HANA(S4);1000
VDI/Full Clone / MCS (Citrix);30
VDI/Linked Clone / PVS (Citrix);50
Virtual Machines/VMware / Hyper-V / KVM - No Database, File nor Email;50
File/General Purpose;100
Unknown (Reducible)/Unknown (Reducible);50
```

2. In `layout_models.py`, refactor the existing `DEFAULT_IOPS_BY_WORKLOAD` dict:
   - Rename current dict to `_DEFAULT_IOPS_HARDCODED` (private, used as fallback)
   - Add `import csv` and `from pathlib import Path` at top
   - Compute CSV path: `_IOPS_CSV_PATH = Path(__file__).parent.parent.parent.parent / "samples" / "IOPS.csv"` (same relative depth pattern as DRR.csv path resolution — verify by checking how DRR.csv is loaded in pipeline)
   - Add `_load_iops_from_csv(path: Path = _IOPS_CSV_PATH) -> dict[str, float]`:
     - If path doesn't exist, return `dict(_DEFAULT_IOPS_HARDCODED)`
     - Open with `encoding="utf-8"`, use `csv.DictReader(f, delimiter=";")`
     - Strip whitespace from both key and value fields
     - Parse IOPS as float, skip rows with ValueError
     - Return loaded dict if non-empty, else fallback to hardcoded
   - Replace module-level `DEFAULT_IOPS_BY_WORKLOAD = {...}` with `DEFAULT_IOPS_BY_WORKLOAD: dict[str, float] = _load_iops_from_csv()`
   - Keep `_DEFAULT_IOPS_FALLBACK: float = 50.0` unchanged

IMPORTANT: Verify the CSV path resolution by checking how `samples/DRR.csv` is loaded elsewhere in the codebase (likely in `services/drr_table.py` or `calculation.py`). The path depth from `layout_models.py` to project root is: `pipeline/ -> store_predict/ -> src/ -> project_root/`. That's 4 `.parent` calls. Confirm this is correct.

Do NOT change any existing public API — `DEFAULT_IOPS_BY_WORKLOAD` and `_DEFAULT_IOPS_FALLBACK` must remain importable with the same names.
  </action>
  <verify>
Run `rtk pytest tests/test_layout_engine.py -v` — all 46 existing tests must pass (CSV loading is transparent; same values as hardcoded dict).
Run `rtk ruff check src/store_predict/pipeline/layout_models.py` — clean.
Run `rtk mypy src/store_predict/pipeline/layout_models.py` — clean.
  </verify>
  <done>IOPS.csv exists in samples/, layout_models.py loads from CSV at import time, all 46 existing tests pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for CSV loader (load, fallback, whitespace stripping)</name>
  <files>tests/test_layout_engine.py</files>
  <action>
Add a new test class `TestLoadIOPSFromCSV` to `tests/test_layout_engine.py` with these tests:

1. `test_load_iops_from_csv_returns_dict` — Call `_load_iops_from_csv()` with default path. Assert returns a dict with at least 8 entries. Assert `result["Database/Microsoft SQL"] == 500.0`.

2. `test_load_iops_from_csv_fallback_when_missing` — Call `_load_iops_from_csv(Path("/nonexistent/IOPS.csv"))`. Assert returns `_DEFAULT_IOPS_HARDCODED` values (same keys and values).

3. `test_load_iops_from_csv_strips_whitespace` — Create a `tmp_path / "IOPS.csv"` with whitespace around keys and values (e.g., `" Database/Oracle ; 800 "`). Call `_load_iops_from_csv(tmp_path / "IOPS.csv")`. Assert `result["Database/Oracle"] == 800.0` (stripped key matches).

4. `test_load_iops_from_csv_skips_bad_rows` — Create a CSV with one valid row and one row with non-numeric IOPS value (e.g., `"BadCategory;notanumber"`). Assert the valid row is loaded and bad row is skipped.

5. `test_load_iops_from_csv_empty_file_uses_fallback` — Create a CSV with only the header row (no data rows). Assert returns `_DEFAULT_IOPS_HARDCODED`.

Import `_load_iops_from_csv` and `_DEFAULT_IOPS_HARDCODED` from `store_predict.pipeline.layout_models`. Use `tmp_path` fixture for tests that create temp CSV files. Do NOT use unittest.mock (project convention).
  </action>
  <verify>
Run `rtk pytest tests/test_layout_engine.py::TestLoadIOPSFromCSV -v` — all 5 new tests pass.
Run `rtk pytest tests/test_layout_engine.py -v` — all tests pass (46 existing + 5 new = 51).
Run `rtk pytest` — full suite passes, no regressions.
  </verify>
  <done>5 new tests cover CSV loading, fallback, whitespace stripping, bad row handling, and empty file fallback. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `rtk pytest tests/test_layout_engine.py -v` — 51 tests pass
- `rtk ruff check src/store_predict/pipeline/layout_models.py tests/test_layout_engine.py` — clean
- `rtk mypy src/store_predict/pipeline/layout_models.py` — clean
- `python -c "from store_predict.pipeline.layout_models import DEFAULT_IOPS_BY_WORKLOAD; print(len(DEFAULT_IOPS_BY_WORKLOAD))"` — prints 8
- `ls samples/IOPS.csv` — file exists
</verification>

<success_criteria>
- samples/IOPS.csv exists with 8 workload IOPS entries, semicolon-delimited
- layout_models.py loads IOPS from CSV at import time with hardcoded fallback
- 51 tests pass (46 existing + 5 new CSV loader tests)
- No regressions in full test suite
</success_criteria>

<output>
After completion, create `.planning/phases/15-default-iops-and-docs/15-01-SUMMARY.md`
</output>
