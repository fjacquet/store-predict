---
phase: 07-ui-bug-fixes-and-report-enhancements
plan: 05
type: execute
wave: 3
depends_on: [07-01, 07-02, 07-04]
files_modified:
  - tests/test_liveoptics_performance.py
  - tests/test_calculation_enhanced.py
  - tests/test_pdf_enhanced.py
autonomous: true
requirements: [FR-5.1, FR-5.2, FR-5.3, FR-5.4, FR-6.1, FR-6.2, FR-6.3, FR-6.4, FR-6.5]

must_haves:
  truths:
    - "LiveOptics performance parsing tested with real sample file"
    - "8K equivalent IOPS computation tested"
    - "Calculation summary includes VM stats and performance totals (tested)"
    - "PDF generation with performance data produces valid PDF (tested)"
    - "PDF generation without performance data still produces valid PDF (tested)"
    - "Graceful fallback for missing performance sheet tested"
  artifacts:
    - path: "tests/test_liveoptics_performance.py"
      provides: "Performance parsing and 8K IOPS tests"
      contains: "test_parse_liveoptics_performance"
    - path: "tests/test_calculation_enhanced.py"
      provides: "Enhanced calculation summary tests"
      contains: "test_calculation_with_performance"
    - path: "tests/test_pdf_enhanced.py"
      provides: "Enhanced PDF generation tests"
      contains: "test_pdf_with_performance"
  key_links:
    - from: "tests/test_liveoptics_performance.py"
      to: "src/store_predict/pipeline/parsers/liveoptics.py"
      via: "parse_liveoptics_performance import"
      pattern: "parse_liveoptics_performance"
    - from: "tests/test_calculation_enhanced.py"
      to: "src/store_predict/pipeline/calculation.py"
      via: "calculate import"
      pattern: "calculate"
---

<objective>
Add comprehensive tests for all Phase 7 pipeline changes: performance parsing, enhanced calculations, and enhanced PDF generation.

Purpose: Ensure performance data pipeline, 8K IOPS computation, VM statistics, and conditional PDF sections all work correctly with real sample data.

Output: Three test files covering the full Phase 7 pipeline additions.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-01-SUMMARY.md
@.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-02-SUMMARY.md
@.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test LiveOptics performance parsing and 8K IOPS computation</name>
  <files>
    tests/test_liveoptics_performance.py
  </files>
  <action>
    Create tests/test_liveoptics_performance.py using REAL sample files (no mocks):

    1. test_parse_liveoptics_performance_returns_dataframe:
       - Call parse_liveoptics_performance on samples/live-optics.xlsx
       - Assert returned DataFrame has expected columns (vm_name, peak_iops, avg_iops, etc.)
       - Assert non-empty (>0 rows)

    2. test_parse_liveoptics_performance_missing_sheet:
       - Call parse_liveoptics_performance on samples/rvtools.xlsx (no VM Performance sheet)
       - Assert returns empty DataFrame (graceful fallback)

    3. test_liveoptics_xlsx_has_performance_columns:
       - Call parse_liveoptics_xlsx on samples/live-optics.xlsx
       - Assert output DataFrame has peak_iops, iops_8k_equivalent columns
       - Assert at least some peak_iops values are non-NaN

    4. test_liveoptics_csv_has_nan_performance:
       - If a CSV sample exists, parse it and assert performance columns are all NaN
       - Otherwise skip this test

    5. test_rvtools_has_nan_performance:
       - Call parse_rvtools on samples/rvtools.xlsx
       - Assert performance columns exist but all values are NaN

    6. test_8k_equivalent_iops_formula:
       - Parse LiveOptics xlsx, get a row with performance data
       - Verify iops_8k_equivalent approximately equals avg_iops + (avg_throughput_kbs / 8.0)
       - Use pytest.approx for floating point comparison

    7. test_liveoptics_xlsx_has_description:
       - Parse LiveOptics xlsx, check vm_description column exists
       - (May be empty if source doesn't have Description column -- test just checks column exists)

    8. test_rvtools_has_annotation_as_description:
       - Parse RVTools xlsx, check vm_description column exists
       - If Annotation data exists in sample, verify some non-empty values

    Use real sample files at samples/live-optics.xlsx and samples/rvtools.xlsx per project convention.
  </action>
  <verify>
    rtk pytest tests/test_liveoptics_performance.py -v
  </verify>
  <done>8 tests covering performance parsing, graceful fallback, 8K IOPS formula, and description extraction. All pass with real sample files.</done>
</task>

<task type="auto">
  <name>Task 2: Test enhanced calculation and PDF generation</name>
  <files>
    tests/test_calculation_enhanced.py
    tests/test_pdf_enhanced.py
  </files>
  <action>
    Create tests/test_calculation_enhanced.py:

    1. test_calculation_has_vm_stats:
       - Call calculate() with sample row_data (3+ VMs with different provisioned sizes)
       - Assert avg_vm_size_mib = total_provisioned / total_vms
       - Assert largest_vm_name matches VM with highest provisioned_mib
       - Assert largest_vm_provisioned_mib is correct

    2. test_calculation_with_performance_data:
       - Call calculate() with row_data that includes peak_iops, avg_iops, peak_throughput_mbs, iops_8k_equivalent
       - Assert has_performance_data is True
       - Assert total_peak_iops = sum of peak_iops
       - Assert peak_throughput_mbs = max of peak_throughput_mbs values

    3. test_calculation_without_performance_data:
       - Call calculate() with row_data missing performance fields
       - Assert has_performance_data is False
       - Assert total_peak_iops is 0.0

    4. test_calculation_empty_data:
       - Call calculate() with empty list
       - Assert all new fields have sensible defaults (0.0, "", False)

    Create tests/test_pdf_enhanced.py:

    5. test_pdf_with_vm_stats:
       - Generate PDF from a CalculationSummary with VM stats filled
       - Assert PDF bytes are non-empty
       - Assert PDF contains "VM Statistics" or "Average VM Size" text (search bytes)

    6. test_pdf_with_performance_section:
       - Generate PDF from CalculationSummary with has_performance_data=True and IOPS values
       - Assert PDF bytes contain "Performance" text

    7. test_pdf_without_performance_section:
       - Generate PDF from CalculationSummary with has_performance_data=False
       - Assert PDF bytes do NOT contain "Performance Summary" text (section omitted)

    8. test_pdf_french_chars_still_work:
       - Generate PDF with project_name containing French chars (e.g. "Projet Resume")
       - Assert PDF bytes are non-empty (Vera fonts handle accents)

    Use real objects, no mocks. Build CalculationSummary manually with all required fields.
  </action>
  <verify>
    rtk pytest tests/test_calculation_enhanced.py tests/test_pdf_enhanced.py -v
    rtk pytest tests/ -x -q (full suite)
  </verify>
  <done>8 tests covering enhanced calculation (VM stats + performance) and enhanced PDF (conditional sections). Full test suite passes.</done>
</task>

</tasks>

<verification>
- rtk pytest tests/ -x -q (full suite passes, including all new tests)
- rtk ruff check tests/
- Total new tests from Phase 7: ~25 tests across 4 test files
</verification>

<success_criteria>
- Performance parsing tested with real LiveOptics sample
- Graceful fallback tested for RVTools and CSV
- 8K equivalent IOPS formula verified
- VM statistics in calculation tested
- PDF conditional sections tested (with and without performance)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-bug-fixes-and-report-enhancements/07-05-SUMMARY.md`
</output>
