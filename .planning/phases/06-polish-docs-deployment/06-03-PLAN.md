---
phase: 06-polish-docs-deployment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_performance.py
autonomous: true
requirements:
  - NFR-4.1
  - NFR-4.2

must_haves:
  truths:
    - "Classification of 5000 VMs completes in under 10 seconds"
    - "PDF generation for a large summary completes in under 5 seconds"
  artifacts:
    - path: "tests/test_performance.py"
      provides: "Performance benchmark tests for classification and PDF generation"
      min_lines: 40
  key_links:
    - from: "tests/test_performance.py"
      to: "src/store_predict/pipeline/classification.py"
      via: "classify_dataframe() with 5000-row DataFrame"
      pattern: "classify_dataframe"
    - from: "tests/test_performance.py"
      to: "src/store_predict/services/pdf_report.py"
      via: "generate_report() with large summary"
      pattern: "generate_report"
---

<objective>
Create performance benchmark tests proving the application handles large workloads (5000 VMs) within acceptable time limits.

Purpose: Verify NFR-4.1 (5000 VMs without timeout) and NFR-4.2 (PDF generation under 5 seconds) with measurable tests.
Output: Performance test file with two benchmark tests.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store_predict/pipeline/classification.py
@src/store_predict/services/pdf_report.py
@src/store_predict/services/calculation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create performance benchmark tests for classification and PDF generation</name>
  <files>tests/test_performance.py</files>
  <action>
Create `tests/test_performance.py` with two tests:

**Test 1: `test_classification_5000_vms_under_10s`**

- Create a synthetic DataFrame with 5000 rows using a `make_large_dataframe(n=5000)` helper.
- Columns: `vm_name`, `os`, `provisioned_mib`, `in_use_mib`, `is_template` (all False).
- Mix VM names: include SQL, Oracle, SAP, VDI, Exchange, generic Windows Server names to exercise multiple rules.
- Mix OS values: `Microsoft Windows Server 2019`, `Red Hat Enterprise Linux 8`, `Ubuntu 22.04`.
- Create a `RuleRegistry(build_default_rules())` and call `classify_dataframe(df, registry)`.
- Use `time.perf_counter()` to measure elapsed time.
- Assert: `len(result) == 5000` and `elapsed < 10.0`.

**Test 2: `test_pdf_generation_under_5s`**

- Import the PDF generation function (check actual function name in `pdf_report.py` -- likely `generate_report`).
- Import `SizingSummary` and `WorkloadGroup` from the calculation module.
- Create a `SizingSummary` with 15 workload groups (simulating a large report with realistic data).
- Use `time.perf_counter()` to measure elapsed time of PDF generation to a temp file.
- Assert: output file exists, file size > 0, `elapsed < 5.0`.
- Clean up temp file in a `finally` block.

Use real objects and imports -- NO mocks (per project convention).
Import paths to verify before writing:

- `from store_predict.pipeline.classification import RuleRegistry, build_default_rules, classify_dataframe`
- Check `src/store_predict/services/pdf_report.py` and `src/store_predict/services/calculation.py` for the actual class/function names.
  </action>
  <verify>Run `rtk pytest tests/test_performance.py -v`. Both tests pass within time limits. Run `rtk ruff check tests/test_performance.py`.</verify>
  <done>Performance tests prove: 5000 VM classification under 10s, PDF generation under 5s. Both use real objects with synthetic data.</done>
</task>

</tasks>

<verification>
1. `tests/test_performance.py` exists with both benchmark tests
2. `rtk pytest tests/test_performance.py -v` passes
3. Classification test uses 5000 rows and asserts < 10 seconds
4. PDF test creates a file and asserts < 5 seconds
5. `rtk ruff check tests/test_performance.py` passes
</verification>

<success_criteria>
Both performance benchmarks pass, proving the application handles 5000 VMs and generates PDFs within acceptable time limits.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-docs-deployment/06-03-SUMMARY.md`
</output>
