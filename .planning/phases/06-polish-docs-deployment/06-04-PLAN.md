---
phase: 06-polish-docs-deployment
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture.md
  - docs/getting-started.md
  - docs/index.md
  - mkdocs.yml
  - README.md
autonomous: true
requirements:
  - NFR-3.1
  - NFR-3.3

must_haves:
  truths:
    - "Architecture page has Mermaid diagrams showing pipeline flow"
    - "Getting-started page has Docker quickstart and local dev instructions"
    - "README.md has quickstart and link to docs"
    - "MkDocs nav includes architecture and getting-started pages"
  artifacts:
    - path: "docs/architecture.md"
      provides: "Architecture overview with Mermaid diagrams"
      min_lines: 40
    - path: "docs/getting-started.md"
      provides: "Quickstart guide for Docker and local dev"
      min_lines: 30
    - path: "README.md"
      provides: "Project root README with quickstart"
      min_lines: 20
    - path: "mkdocs.yml"
      provides: "Updated nav with new pages"
      contains: "architecture.md"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/architecture.md"
      via: "nav entry"
      pattern: "architecture"
    - from: "mkdocs.yml"
      to: "docs/getting-started.md"
      via: "nav entry"
      pattern: "getting-started"
---

<objective>
Create MkDocs documentation pages (architecture with Mermaid diagrams, getting-started guide) and project README.

Purpose: Provide comprehensive documentation for users and developers, with Mermaid diagrams for visual architecture understanding.
Output: Architecture page, getting-started guide, updated mkdocs.yml nav, project README.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mkdocs.yml
@docs/index.md
@src/store_predict/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create architecture documentation with Mermaid diagrams</name>
  <files>docs/architecture.md</files>
  <action>
Create `docs/architecture.md` with:

1. **Overview section** describing StorePredict as a 3-stage pipeline tool for Dell pre-sales.

2. **Pipeline Architecture** Mermaid diagram (flowchart TD):
   - User uploads .xlsx/.csv -> Ingestion Pipeline
   - Ingestion: Format Detection -> parse_rvtools / parse_liveoptics_xlsx / parse_liveoptics_csv
   - All parsers -> Normalized DataFrame
   - Classification: RuleRegistry with 29 rules -> Workload Categories
   - DRR Lookup from DRR.csv
   - Review Page with AG Grid -> User overrides
   - Calculation Engine -> SizingSummary
   - PDF Report Generation (ReportLab)

3. **Data Flow** Mermaid diagram (flowchart LR):
   - Upload (.xlsx/.csv) -> DataFrame (9 canonical columns) -> Classified DataFrame -> SizingSummary -> PDF

4. **Key Components** section listing:
   - Parsers: `pipeline/parsers/rvtools.py`, `pipeline/parsers/liveoptics.py`
   - Classification: `pipeline/classification.py` (29 rules, priority-ordered)
   - DRR Table: `services/drr_table.py` (loaded from `samples/DRR.csv`)
   - Calculation: `services/calculation.py`
   - PDF Report: `services/pdf_report.py`
   - Session State: `ui/state.py` (tab-scoped via `app.storage.tab`)

5. **Technology Stack** section: NiceGUI, pandas, ReportLab, AG Grid, Tailwind CSS.

6. **Session Model** Mermaid diagram showing tab-scoped storage.

Use ```` ```mermaid ```` fences (already configured in mkdocs.yml via pymdownx.superfences).
  </action>
  <verify>Verify `docs/architecture.md` exists, contains ```` ```mermaid ```` blocks, and covers pipeline, data flow, and components.</verify>
  <done>Architecture page with 3 Mermaid diagrams covers the full pipeline from upload to PDF.</done>
</task>

<task type="auto">
  <name>Task 2: Create getting-started guide, README, and update mkdocs nav</name>
  <files>docs/getting-started.md, README.md, mkdocs.yml, docs/index.md</files>
  <action>
**Create `docs/getting-started.md`:**
- Docker Quickstart: `docker compose up --build`, open `http://localhost:8080`
- Environment variables: `STORAGE_SECRET` (required for production)
- Local Development: `uv venv .venv && source .venv/bin/activate`, `uv pip install -e ".[dev]"`, `python -m store_predict.main`
- Running Tests: `pytest`, `ruff check .`, `mypy src/`
- Supported file formats: RVTools .xlsx, LiveOptics .xlsx/.csv

**Create `README.md`:**

- Project title and one-line description
- Quickstart (Docker): 3 lines
- Quickstart (Local): 4 lines
- Link to full docs: `https://fjacquet.github.io/store-predict/`
- Features bullet list: File format detection, 29 classification rules, DRR prediction, PDF report
- License section (if applicable, leave as placeholder)

**Update `mkdocs.yml` nav:**
Add to the nav list:

```yaml
nav:
  - Home: index.md
  - Getting Started: getting-started.md
  - Architecture: architecture.md
  - User Guide:
      ...existing...
```

**Update `docs/index.md`:**

- Add brief intro text and links to Getting Started and Architecture pages if not already present.
  </action>
  <verify>Verify all files exist. Run `rtk ruff check .` (Python files only). Verify mkdocs.yml nav includes `architecture.md` and `getting-started.md`.</verify>
  <done>Documentation site has getting-started guide, architecture page, and project README. MkDocs nav updated.</done>
</task>

</tasks>

<verification>
1. `docs/architecture.md` exists with Mermaid diagrams
2. `docs/getting-started.md` exists with Docker and local dev instructions
3. `README.md` exists with quickstart
4. `mkdocs.yml` nav includes new pages
5. All Mermaid diagrams use ```` ```mermaid ```` fence syntax
</verification>

<success_criteria>
MkDocs documentation site has architecture page with Mermaid diagrams, getting-started guide, and project README with quickstart.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-docs-deployment/06-04-SUMMARY.md`
</output>
