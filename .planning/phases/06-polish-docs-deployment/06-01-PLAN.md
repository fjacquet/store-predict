---
phase: 06-polish-docs-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .dockerignore
  - Dockerfile
  - docker-compose.yml
  - src/store_predict/main.py
autonomous: true
requirements:
  - NFR-1.1
  - NFR-1.2
  - NFR-1.3

must_haves:
  truths:
    - "Docker build excludes .venv, .git, tests, docs from build context"
    - "STORAGE_SECRET is read from environment variable, not hardcoded"
    - "Docker container has a health check on port 8080"
    - "App serves on port 8080 by default"
    - "No external database is required -- all state is in-memory per session"
  artifacts:
    - path: ".dockerignore"
      provides: "Build context exclusions"
      contains: ".venv/"
    - path: "Dockerfile"
      provides: "Production container with health check"
      contains: "HEALTHCHECK"
    - path: "docker-compose.yml"
      provides: "Production-ready compose config"
      contains: "STORAGE_SECRET"
    - path: "src/store_predict/main.py"
      provides: "Env-var storage secret"
      contains: "os.environ"
  key_links:
    - from: "docker-compose.yml"
      to: "src/store_predict/main.py"
      via: "STORAGE_SECRET environment variable"
      pattern: "STORAGE_SECRET"
---

<objective>
Harden Docker deployment for production readiness: add `.dockerignore`, health check, and environment-variable storage secret.

Purpose: Make `docker compose up --build` produce a secure, fast-building production container.
Output: Updated Dockerfile, docker-compose.yml, .dockerignore, and main.py with env-var secret.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Dockerfile
@docker-compose.yml
@src/store_predict/main.py
@src/store_predict/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .dockerignore and add Dockerfile health check</name>
  <files>.dockerignore, Dockerfile</files>
  <action>
Create `.dockerignore` with exclusions for: `.venv/`, `.git/`, `.github/`, `.planning/`, `.mypy_cache/`, `.pytest_cache/`, `.ruff_cache/`, `__pycache__/`, `tests/`, `docs/`, `site/`, `*.egg-info/`, `.DS_Store`, `.env`, `.coverage`, `htmlcov/`, `.store-predict.pid`, `.store-predict.log`.

Add a HEALTHCHECK directive to the Dockerfile after the EXPOSE line:

```dockerfile
HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
  CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8080')" || exit 1
```

The existing Dockerfile structure (FROM, WORKDIR, COPY, RUN, EXPOSE, CMD) should remain intact. Insert HEALTHCHECK between EXPOSE and CMD.
  </action>
  <verify>Verify `.dockerignore` exists and contains `.venv/`. Verify Dockerfile contains HEALTHCHECK directive. Run `rtk ruff check .` to ensure no lint issues.</verify>
  <done>.dockerignore excludes build-bloating directories; Dockerfile has health check on port 8080.</done>
</task>

<task type="auto">
  <name>Task 2: Read STORAGE_SECRET from environment variable</name>
  <files>src/store_predict/main.py, docker-compose.yml</files>
  <action>
In `src/store_predict/main.py`:
1. Add `import os` at the top.
2. In the `main()` function, replace the hardcoded `storage_secret="change-me-in-production"` with:
   ```python
   storage_secret = os.environ.get("STORAGE_SECRET", "dev-only-not-for-production")
   ```
3. Pass this variable to `ui.run(storage_secret=storage_secret, ...)`.

In `docker-compose.yml`:

- The `STORAGE_SECRET` env var is already declared. Verify it is present. Update the default value comment to clarify the user should change it:

  ```yaml
  environment:
    - STORAGE_SECRET=${STORAGE_SECRET:-change-me-in-production}
  ```

This uses Docker Compose variable substitution so the user can set it via `.env` file or shell export.

NFR-1.2: Verify `APP_PORT = 8080` already exists in `config.py` (it does, no change needed).
NFR-1.3: Verify `app.storage.tab` is used for session data (it is via `state.py`, no change needed).
  </action>
  <verify>Run `rtk ruff check src/store_predict/main.py` and `rtk mypy src/store_predict/main.py`. Verify `os.environ.get("STORAGE_SECRET"` appears in main.py.</verify>
  <done>STORAGE_SECRET read from environment with dev fallback. docker-compose.yml supports external secret injection. Port 8080 confirmed. In-memory session confirmed.</done>
</task>

</tasks>

<verification>
1. `.dockerignore` exists and contains `.venv/`, `.git/`, `tests/`, `docs/`
2. `Dockerfile` contains `HEALTHCHECK` directive
3. `main.py` reads `STORAGE_SECRET` from `os.environ`
4. `docker-compose.yml` has `STORAGE_SECRET` variable substitution
5. `rtk ruff check .` passes
6. `rtk mypy src/` passes
</verification>

<success_criteria>
Docker deployment is production-hardened: fast builds (via .dockerignore), env-var secret (not hardcoded), health check for container orchestration, port 8080 confirmed, in-memory session confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-docs-deployment/06-01-SUMMARY.md`
</output>
