---
phase: 03-workload-classification-engine
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/test_classification_integration.py
autonomous: true

must_haves:
  truths:
    - "610 LiveOptics VMs classified with <20% Unknown (Reducible)"
    - "Every rule's category/subcategory pair exists in DRR.csv"
    - "Every DRR category (except Custom DRR) has at least one matching rule"
    - "Known VM patterns from sample data classify correctly"
    - "Classification adds correct DRR-compatible columns to ingestion output"
  artifacts:
    - path: "tests/test_classification_integration.py"
      provides: "Integration tests with real sample data and DRR table consistency"
      min_lines: 60
  key_links:
    - from: "src/store_predict/pipeline/classification.py"
      to: "src/store_predict/services/drr_table.py"
      via: "Every rule category/subcategory must exist in DRRTable"
      pattern: "drr_table.*entries|get_ratio"
    - from: "src/store_predict/pipeline/classification.py"
      to: "src/store_predict/pipeline/ingestion.py"
      via: "classify_dataframe consumes ingest_file output"
      pattern: "ingest_file.*classify_dataframe"
---

<objective>
Validate classification engine against real sample data (610 LiveOptics VMs) and verify rule-DRR table consistency. Ensure every rule references valid DRR categories and that classification achieves >80% reasonable matches.

Purpose: Prove the classification engine works end-to-end with real data, not just synthetic test cases.
Output: Integration test file with sample data validation and DRR consistency checks.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workload-classification-engine/03-RESEARCH.md
@.planning/phases/03-workload-classification-engine/03-01-SUMMARY.md
@src/store_predict/pipeline/classification.py
@src/store_predict/services/drr_table.py
@src/store_predict/pipeline/ingestion.py
@samples/DRR.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests with real sample data</name>
  <files>tests/test_classification_integration.py</files>
  <action>
Create `tests/test_classification_integration.py` with integration tests using real sample files (NO mocks):

1. **DRR table consistency tests** (use samples/DRR.csv fixture):
   - `test_rule_categories_exist_in_drr_table`: Load DRRTable from samples/DRR.csv. For every rule in build_default_rules(), assert (rule.category, rule.subcategory) exists in the DRR table's entries. This catches typos in subcategory strings.
   - `test_all_drr_categories_have_rules`: Load DRRTable, get all (category, subcategory) pairs. Assert every pair has at least one rule in build_default_rules() — except ("Custom DRR", "Custom DRR") which is user-assigned only.

2. **LiveOptics sample classification** (use samples/live-optics.xlsx fixture):
   - `test_classify_liveoptics_sample_no_nulls`: ingest_file() + classify_dataframe() -> all 4 classification columns are non-null for every row.
   - `test_classify_liveoptics_sample_unknown_under_20pct`: Unknown (Reducible) category < 20% of total VMs. This is the primary success criterion from ROADMAP.md.
   - `test_classify_liveoptics_sample_distribution`: Print classification distribution (value_counts) for debugging. Assert at least 5 distinct categories present (sample has Database, VDI, Logging, Virtual Machines, etc.).
   - `test_classify_liveoptics_sql_vms`: VMs with "SQL" in name should all be classified as Database.
   - `test_classify_liveoptics_citrix_vms`: VMs with "CIT" in name should all be classified as VDI.
   - `test_classify_liveoptics_fortinet_vms`: VMs with FortiNet OS should be Logging-Analytics.

3. **RVTools sample classification** (use samples/rvtools.xlsx if available):
   - `test_classify_rvtools_sample_no_nulls`: Same as LiveOptics — all columns non-null.

4. **End-to-end pipeline test:**
   - `test_ingest_then_classify_pipeline`: Load file, classify, then verify DRR lookup works for every classified VM by calling drr_table.get_ratio(category, subcategory) and asserting ratio > 0.

Fixtures: Use conftest.py fixtures for sample file paths and DRRTable. If fixtures don't exist yet, create them in the test file using `pathlib.Path(__file__).parent.parent / "samples"` paths.

Important: These tests use REAL sample files. They validate the classification engine against actual customer VM naming patterns, which is the core quality gate.
  </action>
  <verify>
Run `rtk pytest tests/test_classification_integration.py -v` — all tests pass.
Run `rtk pytest` — full suite passes.
Print classification distribution: `python -c "from pathlib import Path; from store_predict.pipeline.ingestion import ingest_file; from store_predict.pipeline.classification import RuleRegistry, build_default_rules, classify_dataframe; df = ingest_file(Path('samples/live-optics.xlsx')); r = classify_dataframe(df, RuleRegistry(build_default_rules())); print(r['workload_category'].value_counts())"` — shows reasonable distribution.
  </verify>
  <done>Integration tests pass: all rule categories exist in DRR table, all DRR categories have rules, 610 VMs classified with <20% Unknown, SQL/Citrix/FortiNet VMs correctly classified. Full test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Print classification report and verify coverage</name>
  <files>tests/test_classification_integration.py</files>
  <action>
Add a final test to `tests/test_classification_integration.py`:

- `test_classification_coverage_report`: Classify all 610 LiveOptics VMs, print a summary table showing: category, count, percentage, sample VM names (first 2 per category). This is not a pass/fail test — it uses `print()` to produce a human-readable report for verification. Assert the total count equals the input count (no VMs lost).

Also verify that the confidence breakdown is reasonable:
- "rule_match" should be present (VMs matched by specific rules)
- "os_fallback" should be present (generic VMs caught by OS)
- "default" should be minimal (<5%)

Run `rtk pytest tests/test_classification_integration.py -v -s` to see the report output during test run.
  </action>
  <verify>
Run `rtk pytest tests/test_classification_integration.py::test_classification_coverage_report -v -s` — prints coverage report, test passes.
Run `rtk pytest` — full suite passes (43 existing + all new tests).
  </verify>
  <done>Coverage report test exists and produces readable output. Confidence breakdown shows rule_match, os_fallback, and minimal default. Full test suite green with 60+ tests total.</done>
</task>

</tasks>

<verification>
- `rtk pytest tests/test_classification_integration.py -v` — all integration tests pass
- `rtk pytest` — full suite passes with no regressions
- `rtk ruff check tests/test_classification_integration.py` — lint clean
- Classification distribution shows >80% reasonable matches (Virtual Machines + specific categories dominate, Unknown < 20%)
</verification>

<success_criteria>
- Every rule's (category, subcategory) verified against DRR.csv
- Every DRR category (except Custom DRR) has at least one rule
- 610 LiveOptics VMs classified with <20% Unknown (Reducible)
- SQL VMs -> Database, Citrix VMs -> VDI, FortiNet VMs -> Logging-Analytics
- DRR ratio lookup works for every classified VM
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-workload-classification-engine/03-02-SUMMARY.md`
</output>
