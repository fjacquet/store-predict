---
phase: 08-i18n-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/store_predict/ui/layout.py
  - src/store_predict/ui/pages/upload.py
  - src/store_predict/ui/pages/review.py
  - src/store_predict/ui/pages/report.py
  - src/store_predict/ui/components/vm_table.py
  - src/store_predict/ui/components/summary_stats.py
  - src/store_predict/ui/components/workload_dialog.py
  - src/store_predict/ui/components/dark_mode_toggle.py
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - I18N-05

must_haves:
  truths:
    - "All hardcoded user-visible strings in UI files are replaced with t() calls"
    - "The header shows FR/EN locale toggle button next to the dark mode toggle"
    - "AG Grid column headers display translated names (set at construction time via t())"
    - "AG Grid UI chrome (pagination, filter menus) uses French text when locale is 'fr'"
    - "Nav links in the header are translated (Home/Accueil, Upload/Télécharger, etc.)"
    - "No f-string mixing a translated phrase with a variable remains in any UI file"
  artifacts:
    - path: "src/store_predict/ui/layout.py"
      provides: "Shared header with translated nav links and locale_toggle button"
      contains: "add_locale_toggle"
    - path: "src/store_predict/ui/pages/upload.py"
      provides: "Upload page with all 6 strings wrapped in t()"
      contains: "t(\"upload."
    - path: "src/store_predict/ui/pages/review.py"
      provides: "Review page with all 8 strings wrapped in t()"
      contains: "t(\"review."
    - path: "src/store_predict/ui/pages/report.py"
      provides: "Report page with all 14 strings wrapped in t()"
      contains: "t(\"report."
    - path: "src/store_predict/ui/components/vm_table.py"
      provides: "AG Grid with translated headerName values and FR locale pack"
      contains: "localeText"
    - path: "src/store_predict/ui/components/summary_stats.py"
      provides: "Summary stat cards with translated labels"
    - path: "src/store_predict/ui/components/workload_dialog.py"
      provides: "Workload dialog with translated title, hint, and button labels"
    - path: "src/store_predict/ui/components/dark_mode_toggle.py"
      provides: "Dark mode toggle with translated label"
  key_links:
    - from: "src/store_predict/ui/layout.py"
      to: "src/store_predict/ui/components/locale_toggle.py"
      via: "import and call add_locale_toggle() in header row"
      pattern: "add_locale_toggle"
    - from: "src/store_predict/ui/components/vm_table.py"
      to: "CDN @ag-grid-community/locale@32.2.2"
      via: "ui.add_head_html() with defer script tag when locale is 'fr'"
      pattern: "add_head_html.*ag-grid-community"
    - from: "src/store_predict/ui/components/vm_table.py"
      to: "src/store_predict/i18n/__init__.py"
      via: "t('columns.vm_name') etc. in headerName values"
      pattern: "t\\(\"columns\\."
---

<objective>
Wrap all 65 UI-layer strings in `t()` calls across 8 files, wire the locale toggle into the shared header, and configure AG Grid with French locale support.

Purpose: After this plan, every visible label in the NiceGUI interface is served from YAML locale files and switches language on page reload. This satisfies I18N-01 (string wrapping), I18N-02 (header toggle), I18N-03 (AG Grid locale), and I18N-05 (reload-based switch).
Output: 8 modified source files — all user-visible strings gone, replaced with t() calls. AG Grid shows French UI chrome when locale is 'fr'.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-i18n-foundation/08-RESEARCH.md
@.planning/phases/08-i18n-foundation/08-01-SUMMARY.md
@src/store_predict/ui/layout.py
@src/store_predict/ui/pages/upload.py
@src/store_predict/ui/pages/review.py
@src/store_predict/ui/pages/report.py
@src/store_predict/ui/components/vm_table.py
@src/store_predict/ui/components/summary_stats.py
@src/store_predict/ui/components/workload_dialog.py
@src/store_predict/ui/components/dark_mode_toggle.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire locale toggle into header and wrap layout + component strings</name>
  <files>
    src/store_predict/ui/layout.py
    src/store_predict/ui/components/dark_mode_toggle.py
    src/store_predict/ui/components/summary_stats.py
    src/store_predict/ui/components/workload_dialog.py
  </files>
  <action>
Read each file first before editing. Apply the following changes:

**`src/store_predict/ui/layout.py`:**
- Add import: `from store_predict.i18n import t`
- Add import: `from store_predict.ui.components.locale_toggle import add_locale_toggle`
- Replace all 5 hardcoded nav link strings with t() calls:
  - `"Home"` → `t("layout.home")`
  - `"Upload"` → `t("layout.upload")`
  - `"Review"` → `t("layout.review")`
  - `"Report"` → `t("layout.report")`
- Add `add_locale_toggle()` call inside the header row, after `add_dark_mode_toggle()`.
- The `title` parameter to `layout()` can remain as-is (it's a Python arg, not a hardcoded string).

**`src/store_predict/ui/components/dark_mode_toggle.py`:**
- Add import: `from store_predict.i18n import t`
- Replace the hardcoded `"Dark Mode"` string with `t("layout.dark_mode")`.
- Ensure the import is at the top of the file in proper order (isort: stdlib, third-party, first-party).

**`src/store_predict/ui/components/summary_stats.py`:**
- Add import: `from store_predict.i18n import t`
- Replace all 4 stat card label strings:
  - `"Total VMs"` → `t("stats.total_vms")`
  - `"Total Provisioned"` → `t("stats.total_provisioned")`
  - `"Avg DRR"` → `t("stats.avg_drr")`
  - `"Effective Capacity"` → `t("stats.effective_capacity")`
- Note: stat labels are used in `update_stats()` or similar — wrap at the call site, not as module-level constants.

**`src/store_predict/ui/components/workload_dialog.py`:**
- Add import: `from store_predict.i18n import t`
- Replace all 5 dialog strings:
  - Dialog title string containing VM name — must use `t("dialog.workloads_for", vm_name=vm_name)` (NOT an f-string)
  - Hint text → `t("dialog.select_hint")`
  - Label text → `t("dialog.select_label")`
  - Cancel button label → `t("dialog.cancel")`
  - Apply button label → `t("dialog.apply")`
- Ensure no f-string concatenates a translated phrase with a variable. Use named placeholders.

Run `rtk ruff check` and `rtk ruff format` on all 4 files after editing.
  </action>
  <verify>
`python -c "from store_predict.ui.layout import layout; print('layout OK')"` — no ImportError.
`python -c "from store_predict.ui.components.dark_mode_toggle import add_dark_mode_toggle; print('OK')"` — no error.
`rtk ruff check src/store_predict/ui/layout.py src/store_predict/ui/components/` — no errors.
  </verify>
  <done>
`layout.py` imports and calls `add_locale_toggle()`. All 4 files compile cleanly. No hardcoded nav/label strings remain in these files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrap page strings and configure AG Grid with French locale</name>
  <files>
    src/store_predict/ui/pages/upload.py
    src/store_predict/ui/pages/review.py
    src/store_predict/ui/pages/report.py
    src/store_predict/ui/components/vm_table.py
  </files>
  <action>
Read each file first before editing. Apply the following changes:

**`src/store_predict/ui/pages/upload.py`** (6 strings):
- Add import: `from store_predict.i18n import t`
- Wrap all user-visible strings:
  - Page title / heading → `t("upload.title")`
  - Project name label → `t("upload.project_label")`
  - Project name input placeholder → `t("upload.project_placeholder")`
  - Drop zone label → `t("upload.drop_label")`
  - Supported formats text → `t("upload.supported_formats")`
  - Notification on successful load (e.g., "Loaded 42 VMs") → `t("upload.loaded_notify", count=N)` where N is the VM count

**`src/store_predict/ui/pages/review.py`** (8 strings):
- Add import: `from store_predict.i18n import t`
- Wrap all user-visible strings:
  - Page title → `t("review.title")`
  - Project label (with name) → `t("review.project_label", name=project_name)` (NOT f-string)
  - No-data message → `t("review.no_data")`
  - Bulk update button → `t("review.bulk_update")`
  - Generate report button → `t("review.generate_report")`
  - No rows selected notification → `t("review.no_rows_selected")`
  - Updated notification (with count, category, subcategory) → `t("review.updated_notify", count=N, category=cat, subcategory=sub)`

**`src/store_predict/ui/pages/report.py`** (14 strings):
- Add import: `from store_predict.i18n import t`
- Also add: `from store_predict.i18n.locale import get_locale`
- Wrap all user-visible strings across section headings (Totals, Averages, Performance Summary, Workload Breakdown), page title, project label, no-data message, download button, back button.
- Pass locale to PDF generation: where `generate_report_pdf(summary, project_name)` is called, change to `generate_report_pdf(summary, project_name, locale=get_locale())`.

**`src/store_predict/ui/components/vm_table.py`** (12 strings + AG Grid locale):
- Add imports: `from store_predict.i18n import t` and `from store_predict.i18n.locale import get_locale`
- Wrap all 12 `headerName` values in column definitions with `t("columns.<key>")` calls:
  - vm_name, os, description, workload_category, subcategory, drr, provisioned_mib, in_use_mib, confidence, peak_iops, iops_8k, peak_mbs
- Add AG Grid French locale CDN script. In the function that creates the grid (or at the top of the page-level create function), add:
  ```python
  locale = get_locale()
  if locale == "fr":
      cdn_url = (
          "https://cdn.jsdelivr.net/npm/@ag-grid-community/locale@32.2.2"
          "/dist/umd/@ag-grid-community/locale.min.js"
      )
      ui.add_head_html(f'<script src="{cdn_url}" defer></script>')
  ```
- Add `:localeText` to grid options dict ONLY when locale is "fr":
  ```python
  if locale == "fr":
      grid_options[":localeText"] = "AG_GRID_LOCALE_FR"
  ```
  The colon prefix on `:localeText` is the NiceGUI binding syntax that evaluates the value as a JavaScript expression — required so `AG_GRID_LOCALE_FR` resolves as the JS object, not a string literal.

Run `rtk ruff check` and `rtk ruff format` on all 4 files. Then `rtk mypy src/store_predict/ui/` to catch type errors.
  </action>
  <verify>
`python -c "from store_predict.ui.pages.upload import upload_page; print('upload OK')" 2>&1 | head -5` — no ImportError (NiceGUI context errors are expected and acceptable here).
`python -c "from store_predict.ui.components.vm_table import create_vm_table; print('vm_table OK')" 2>&1 | head -5` — no ImportError.
`rtk ruff check src/store_predict/ui/pages/ src/store_predict/ui/components/vm_table.py` — no errors.
`rtk mypy src/store_predict/ui/` — no new errors beyond pre-existing NiceGUI/reportlab stubs.
  </verify>
  <done>
All 65 UI-layer strings are wrapped in t() calls. AG Grid is configured with CDN locale script and `:localeText` binding for French. `generate_report_pdf()` is called with `locale=get_locale()`. All files pass ruff and mypy.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite to confirm nothing is broken:
`rtk pytest tests/` — all existing 121+ tests must still pass (i18n wrapping must not break any existing behavior since get_locale() returns 'fr' by default in test context, and all strings still render).

Check no hardcoded strings remain:
`rtk grep "\"Home\"\|\"Upload\"\|\"Review\"\|\"Report\"\|\"Dark Mode\"\|\"Total VMs\"" src/store_predict/ui/` — should return zero matches in modified files (only in YAML and tests is acceptable).

Check locale_toggle is wired in layout:
`rtk grep "add_locale_toggle" src/store_predict/ui/layout.py` — must show one match.

Check AG Grid localeText:
`rtk grep "localeText\|AG_GRID_LOCALE" src/store_predict/ui/components/vm_table.py` — must show both strings.
</verification>

<success_criteria>
- All 65 user-visible strings in 8 UI files replaced with t() calls
- layout.py imports and calls add_locale_toggle() in the header
- vm_table.py loads AG Grid French locale CDN script and sets :localeText for fr locale
- report.py passes locale=get_locale() to generate_report_pdf()
- Full pytest suite passes (121+ tests, all green)
- ruff check and mypy pass on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/08-i18n-foundation/08-02-SUMMARY.md` following the template at `@/Users/fjacquet/.claude/get-shit-done/templates/summary.md`.
</output>
