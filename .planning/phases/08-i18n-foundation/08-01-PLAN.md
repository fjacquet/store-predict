---
phase: 08-i18n-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/store_predict/i18n/__init__.py
  - src/store_predict/i18n/locale.py
  - src/store_predict/i18n/locales/en.yaml
  - src/store_predict/i18n/locales/fr.yaml
  - src/store_predict/ui/components/locale_toggle.py
autonomous: true
requirements:
  - I18N-01
  - I18N-02

must_haves:
  truths:
    - "Calling t('upload.title') returns 'Upload Workload Data' when locale is 'en'"
    - "Calling t('upload.title') returns 'Téléchargement des données' when locale is 'fr'"
    - "get_locale() returns 'fr' without raising when called outside a NiceGUI request context"
    - "t() with a placeholder (e.g., count=42) substitutes the value correctly in both locales"
    - "The locale_toggle component exists and can be imported without error"
    - "python-i18n[YAML] is declared as a project dependency in pyproject.toml"
  artifacts:
    - path: "src/store_predict/i18n/__init__.py"
      provides: "t() helper and i18n configuration"
      exports: ["t", "configure_i18n"]
    - path: "src/store_predict/i18n/locale.py"
      provides: "get_locale() and set_locale() session helpers"
      exports: ["get_locale", "set_locale"]
    - path: "src/store_predict/i18n/locales/en.yaml"
      provides: "English translations for all 73 strings"
      contains: "upload, review, report, columns, stats, pdf, layout, dialog"
    - path: "src/store_predict/i18n/locales/fr.yaml"
      provides: "French translations for all 73 strings"
      contains: "upload, review, report, columns, stats, pdf, layout, dialog"
    - path: "src/store_predict/ui/components/locale_toggle.py"
      provides: "FR/EN toggle button component"
      exports: ["add_locale_toggle"]
  key_links:
    - from: "src/store_predict/i18n/__init__.py"
      to: "src/store_predict/i18n/locale.py"
      via: "lazy import of get_locale() inside t() to avoid circular import"
      pattern: "from store_predict\\.i18n\\.locale import get_locale"
    - from: "src/store_predict/i18n/__init__.py"
      to: "src/store_predict/i18n/locales/"
      via: "i18n.set('load_path', [str(_LOCALES_DIR)])"
      pattern: "i18n\\.set.*load_path"
    - from: "src/store_predict/ui/components/locale_toggle.py"
      to: "src/store_predict/i18n/locale.py"
      via: "get_locale() + set_locale() then ui.run_javascript('location.reload()')"
      pattern: "set_locale.*location\\.reload"
---

<objective>
Build the i18n infrastructure: install dependency, create the `i18n` package with the `t()` helper, write English and French YAML locale files covering all 73 strings, and create the locale toggle component.

Purpose: Every subsequent plan in this phase (and every future feature) depends on `t()` existing and working correctly. This plan creates the foundation — no UI strings are wrapped yet.
Output: Working `t()` helper, complete YAML locale files, `locale_toggle` component, and `python-i18n[YAML]` declared in pyproject.toml.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-i18n-foundation/08-RESEARCH.md
@src/store_predict/ui/layout.py
@src/store_predict/ui/components/dark_mode_toggle.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add python-i18n dependency and create i18n package</name>
  <files>
    pyproject.toml
    src/store_predict/i18n/__init__.py
    src/store_predict/i18n/locale.py
  </files>
  <action>
1. Edit `pyproject.toml`:
   - Add `"python-i18n[YAML]>=0.3.9"` to the `dependencies` list (after reportlab entry).
   - Add `"store_predict = ['data/*.csv', 'i18n/locales/*.yaml']"` to `[tool.setuptools.package-data]` (replace the existing single-key entry to include both globs). The YAML files must ship with the package.
   - Add a mypy override block after the reportlab one:
     ```toml
     [[tool.mypy.overrides]]
     module = "i18n.*"
     ignore_missing_imports = true
     ```
   Then run: `uv pip install -e ".[dev]"` to install the new dependency.

2. Create `src/store_predict/i18n/__init__.py`:
   ```python
   """i18n package: t() helper and configuration for python-i18n."""
   from __future__ import annotations

   from pathlib import Path

   import i18n

   _LOCALES_DIR = Path(__file__).parent / "locales"

   # Configure once at import time — settings are idempotent globals
   i18n.set("load_path", [str(_LOCALES_DIR)])
   i18n.set("fallback", "en")
   i18n.set("filename_format", "{locale}.{format}")
   i18n.set("file_format", "yaml")
   i18n.set("skip_locale_root_data", True)  # keys NOT prefixed with locale name in YAML


   def t(key: str, **kwargs: object) -> str:
       """Return translated string for the current tab's locale.

       Reads locale from app.storage.tab on every call (tab-scoped, not global).
       Sets the python-i18n process-global locale immediately before the lookup —
       safe because NiceGUI's async event loop is single-threaded: no other coroutine
       can interleave within one synchronous t() call.

       Falls back to English if locale is unset or missing key in French.
       """
       from store_predict.i18n.locale import get_locale  # lazy to avoid circular import

       locale = get_locale()
       i18n.set("locale", locale)
       return str(i18n.t(key, **kwargs))


   __all__ = ["t"]
   ```

3. Create `src/store_predict/i18n/locale.py`:
   ```python
   """Per-session locale helpers backed by app.storage.tab."""
   from __future__ import annotations

   _DEFAULT_LOCALE = "fr"  # French is the primary use-case language


   def get_locale() -> str:
       """Return the current tab's locale from NiceGUI session storage.

       Falls back to French (primary user language) when:
       - locale key not yet set in this tab's storage
       - called outside a NiceGUI request context (e.g., pytest, scripts)

       The RuntimeError catch is intentional: it makes every function using t()
       safely callable in unit tests without a live NiceGUI server.
       """
       try:
           from nicegui import app

           return str(app.storage.tab.get("locale", _DEFAULT_LOCALE))
       except RuntimeError:
           # Outside NiceGUI request context (e.g., pytest, CLI scripts)
           return _DEFAULT_LOCALE


   def set_locale(locale: str) -> None:
       """Persist locale choice to tab-scoped session storage."""
       from nicegui import app

       app.storage.tab["locale"] = locale


   __all__ = ["get_locale", "set_locale"]
   ```
  </action>
  <verify>
Run: `uv pip show python-i18n` — must show version 0.3.9 or higher.
Run: `python -c "from store_predict.i18n import t; from store_predict.i18n.locale import get_locale; print(get_locale())"` — must print `fr` without error.
  </verify>
  <done>
`python-i18n[YAML]` is installed and importable. `t()` and `get_locale()` are importable from `store_predict.i18n` without error. `get_locale()` returns `'fr'` when called with no NiceGUI context.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write YAML locale files and locale_toggle component</name>
  <files>
    src/store_predict/i18n/locales/en.yaml
    src/store_predict/i18n/locales/fr.yaml
    src/store_predict/ui/components/locale_toggle.py
  </files>
  <action>
1. Create `src/store_predict/i18n/locales/en.yaml` with all 73 strings across 8 namespaces.
   Use the exact key structure from the research file (Pattern 2). Key namespaces: upload, review, report, columns, stats, pdf, layout, dialog.
   Important notes:
   - `layout.language` must be `"FR"` in en.yaml (shows the language you will SWITCH TO).
   - Placeholder format: `%{variable_name}` (not {}, not %(name)s).
   - Quote YAML strings that start with special chars or contain colons.
   - Do NOT use pluralization syntax (`one:` / `other:`) — use "(s)" workaround per research.

   Full content (copy exactly from the research YAML under Pattern 2):
   ```yaml
   upload:
     title: Upload Workload Data
     project_label: Project Name
     project_placeholder: "e.g., Customer-DC-Migration-2026"
     drop_label: "Drop RVTools or LiveOptics file here (.xlsx, .csv)"
     supported_formats: "Supported formats: RVTools (.xlsx), LiveOptics (.xlsx, .csv)"
     loaded_notify: "Loaded %{count} VM(s)"

   review:
     title: Review Classifications
     project_label: "Project: %{name}"
     no_data: No data uploaded yet.
     bulk_update: Bulk Update Workload
     generate_report: Generate Report
     no_rows_selected: "No rows selected. Use checkboxes to select VMs first."
     updated_notify: "Updated %{count} VM(s) to %{category} / %{subcategory}"

   report:
     title: Sizing Report
     project_label: "Project: %{name}"
     no_data: No data available. Please upload a file first.
     totals_heading: Totals
     averages_heading: Averages
     performance_heading: Performance Summary
     breakdown_heading: Workload Breakdown
     download_pdf: Download PDF Report
     back_to_review: Back to Review

   columns:
     vm_name: VM Name
     os: OS
     description: Description
     workload_category: Workload Category
     subcategory: Subcategory
     drr: DRR
     provisioned_mib: "Provisioned (MiB)"
     in_use_mib: "In Use (MiB)"
     confidence: Confidence
     peak_iops: Peak IOPS
     iops_8k: "8K Eq. IOPS"
     peak_mbs: "Peak MB/s"

   stats:
     total_vms: Total VMs
     total_provisioned: Total Provisioned
     avg_drr: Avg DRR
     effective_capacity: Effective Capacity

   pdf:
     report_title: StorePredict Sizing Report
     total_vms: "Total VMs:"
     total_cpus: "Total vCPUs:"
     total_memory: "Total Memory:"
     total_provisioned: "Total Provisioned:"
     total_in_use: "Total In Use:"
     required_capacity: "Required Capacity:"
     avg_cpus: "Avg vCPUs / VM:"
     avg_memory: "Avg Memory / VM:"
     avg_storage: "Avg Storage / VM:"
     weighted_drr: "Weighted Avg DRR:"
     largest_vm: "Largest VM:"
     total_avg_iops: "Total Average IOPS:"
     hottest_vm: "Hottest VM Peak IOPS:"
     peak_throughput: "Peak Throughput:"
     iops_8k: "Total 8K Equivalent IOPS:"
     table_category: Category
     table_vms: VMs
     table_provisioned: "Provisioned (GiB)"
     table_avg_drr: Avg DRR
     table_required: "Required (GiB)"
     table_total: TOTAL
     performance_heading: Performance Summary
     stats_heading: VM Statistics

   layout:
     home: Home
     upload: Upload
     review: Review
     report: Report
     dark_mode: Dark Mode
     language: "FR"

   dialog:
     workloads_for: "Workloads for %{vm_name}"
     select_hint: "Select one or more workload types. Conservative (lowest) DRR will be used."
     select_label: Select workload types
     cancel: Cancel
     apply: Apply
   ```

2. Create `src/store_predict/i18n/locales/fr.yaml` with the French translations. Same key structure as en.yaml. Important: `layout.language` must be `"EN"` (switching to English).

   Full content:
   ```yaml
   upload:
     title: "Téléchargement des données"
     project_label: Nom du projet
     project_placeholder: "ex. : Client-DC-Migration-2026"
     drop_label: "Déposez votre fichier RVTools ou LiveOptics ici (.xlsx, .csv)"
     supported_formats: "Formats supportés : RVTools (.xlsx), LiveOptics (.xlsx, .csv)"
     loaded_notify: "%{count} VM(s) chargées"

   review:
     title: "Révision des classifications"
     project_label: "Projet : %{name}"
     no_data: "Aucune donnée chargée."
     bulk_update: "Mise à jour groupée"
     generate_report: "Générer le rapport"
     no_rows_selected: "Aucune ligne sélectionnée. Utilisez les cases à cocher pour sélectionner des VMs."
     updated_notify: "%{count} VM(s) mises à jour vers %{category} / %{subcategory}"

   report:
     title: "Rapport de dimensionnement"
     project_label: "Projet : %{name}"
     no_data: "Aucune donnée disponible. Veuillez d'abord télécharger un fichier."
     totals_heading: Totaux
     averages_heading: Moyennes
     performance_heading: "Résumé des performances"
     breakdown_heading: "Détail par charge de travail"
     download_pdf: "Télécharger le rapport PDF"
     back_to_review: "Retour à la révision"

   columns:
     vm_name: Nom de la VM
     os: "Système d'exploitation"
     description: Description
     workload_category: "Catégorie de charge"
     subcategory: "Sous-catégorie"
     drr: DRR
     provisioned_mib: "Provisionné (Mio)"
     in_use_mib: "Utilisé (Mio)"
     confidence: Confiance
     peak_iops: IOPS de pointe
     iops_8k: "IOPS éq. 8K"
     peak_mbs: "Débit de pointe (Mo/s)"

   stats:
     total_vms: Total VMs
     total_provisioned: Total provisionné
     avg_drr: DRR moyen
     effective_capacity: "Capacité effective"

   pdf:
     report_title: "Rapport de dimensionnement StorePredict"
     total_vms: "Nombre de VMs :"
     total_cpus: "Total vCPUs :"
     total_memory: "Mémoire totale :"
     total_provisioned: "Total provisionné :"
     total_in_use: "Total utilisé :"
     required_capacity: "Capacité requise :"
     avg_cpus: "vCPUs moy. / VM :"
     avg_memory: "Mémoire moy. / VM :"
     avg_storage: "Stockage moy. / VM :"
     weighted_drr: "DRR moyen pondéré :"
     largest_vm: "VM la plus grande :"
     total_avg_iops: "IOPS moyennes totales :"
     hottest_vm: "IOPS de pointe max :"
     peak_throughput: "Débit de pointe :"
     iops_8k: "IOPS éq. 8K totales :"
     table_category: "Catégorie"
     table_vms: VMs
     table_provisioned: "Provisionné (Gio)"
     table_avg_drr: DRR moyen
     table_required: "Requis (Gio)"
     table_total: TOTAL
     performance_heading: "Résumé des performances"
     stats_heading: "Statistiques des VMs"

   layout:
     home: Accueil
     upload: "Télécharger"
     review: "Révision"
     report: Rapport
     dark_mode: "Mode sombre"
     language: "EN"

   dialog:
     workloads_for: "Charges de travail pour %{vm_name}"
     select_hint: "Sélectionnez un ou plusieurs types de charge. Le DRR le plus conservateur (le plus bas) sera utilisé."
     select_label: "Sélectionner les charges de travail"
     cancel: Annuler
     apply: Appliquer
   ```

3. Create `src/store_predict/ui/components/locale_toggle.py`:
   ```python
   """FR/EN language toggle button for the shared header."""
   from __future__ import annotations

   from nicegui import ui

   from store_predict.i18n import t
   from store_predict.i18n.locale import get_locale, set_locale


   def add_locale_toggle() -> None:
       """Add FR/EN language toggle button to the current layout container.

       The button label shows the language you will SWITCH TO (not current),
       which is the standard UX convention (same as Wikipedia, Google, etc.).

       Clicking writes the new locale to app.storage.tab and reloads the page.
       Full page reload is required because:
       - ui.header cannot be inside @ui.refreshable (NiceGUI 1.5+ limitation)
       - AG Grid does not support dynamic localeText updates on existing grid instances
       """
       current = get_locale()
       next_locale = "en" if current == "fr" else "fr"
       label = t("layout.language")  # "FR" (when current=en) or "EN" (when current=fr)

       def _switch() -> None:
           set_locale(next_locale)
           ui.run_javascript("location.reload()")

       ui.button(label, on_click=_switch).props("flat color=white dense")
   ```
  </action>
  <verify>
Run: `python -c "from store_predict.i18n import t; import store_predict.i18n.locale as loc; import unittest.mock as m; m.patch('store_predict.i18n.locale.get_locale', return_value='en').start(); print(t('upload.title'))"` — should print "Upload Workload Data".

Simpler check without mocking: `python -c "from store_predict.i18n.locale import get_locale; print(get_locale())"` — prints `fr`.

`python -c "from store_predict.ui.components.locale_toggle import add_locale_toggle; print('OK')"` — prints OK.

Check YAML structure: `python -c "import yaml; d=yaml.safe_load(open('src/store_predict/i18n/locales/en.yaml')); print(list(d.keys()))"` — must print `['upload', 'review', 'report', 'columns', 'stats', 'pdf', 'layout', 'dialog']`.
  </verify>
  <done>
Both YAML files exist with all 8 namespaces and correct key structure. `locale_toggle` component is importable. `t('upload.title')` resolves to the correct English string. `t('upload.loaded_notify', count=5)` resolves to a string containing "5".
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `uv pip show python-i18n` reports version >= 0.3.9
2. `python -c "from store_predict.i18n import t; print('i18n package OK')"` — no ImportError
3. `python -c "from store_predict.i18n.locale import get_locale; assert get_locale() == 'fr'; print('get_locale OK')"` — no AssertionError
4. `python -c "import yaml; en=yaml.safe_load(open('src/store_predict/i18n/locales/en.yaml')); fr=yaml.safe_load(open('src/store_predict/i18n/locales/fr.yaml')); assert set(en.keys()) == set(fr.keys()); print('YAML keys match')"` — no AssertionError
5. `rtk ruff check src/store_predict/i18n/` and `rtk ruff check src/store_predict/ui/components/locale_toggle.py` — no errors
6. `rtk mypy src/store_predict/i18n/ src/store_predict/ui/components/locale_toggle.py` — no errors
</verification>

<success_criteria>
- `python-i18n[YAML]>=0.3.9` in `pyproject.toml` dependencies and mypy overrides
- `src/store_predict/i18n/__init__.py` exports `t()` with safe per-tab locale reads
- `src/store_predict/i18n/locale.py` exports `get_locale()` and `set_locale()` with RuntimeError guard
- `src/store_predict/i18n/locales/en.yaml` and `fr.yaml` both present with 8 matching namespaces
- `src/store_predict/ui/components/locale_toggle.py` exports `add_locale_toggle()`
- `pyproject.toml` package-data includes `i18n/locales/*.yaml` so YAML files ship with the package
- All new files pass `ruff check` and `mypy` with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-i18n-foundation/08-01-SUMMARY.md` following the template at `@/Users/fjacquet/.claude/get-shit-done/templates/summary.md`.
</output>
