---
phase: 01-foundation-drr-table
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/store_predict/main.py
  - src/store_predict/ui/layout.py
  - src/store_predict/ui/pages/upload.py
  - Dockerfile
  - docker-compose.yml
autonomous: true

must_haves:
  truths:
    - "python -m store_predict.main starts a web server on port 8080"
    - "Browser shows StorePredict landing page at http://localhost:8080"
    - "Upload page placeholder is accessible at /upload"
    - "docker compose up builds and runs the app successfully"
    - "App inside Docker is reachable at http://localhost:8080"
  artifacts:
    - path: "src/store_predict/main.py"
      provides: "NiceGUI app entry point"
      contains: "ui.run"
    - path: "src/store_predict/ui/layout.py"
      provides: "Shared layout with header/nav"
      contains: "def layout"
    - path: "src/store_predict/ui/pages/upload.py"
      provides: "Upload page placeholder"
      contains: "@ui.page"
    - path: "Dockerfile"
      provides: "Docker build instructions"
      contains: "python:3.12-slim"
    - path: "docker-compose.yml"
      provides: "Docker Compose orchestration"
      contains: "8080:8080"
  key_links:
    - from: "src/store_predict/main.py"
      to: "src/store_predict/ui/pages/upload.py"
      via: "import to register page routes"
      pattern: "import.*upload"
    - from: "src/store_predict/main.py"
      to: "src/store_predict/config.py"
      via: "APP_PORT and APP_TITLE config"
      pattern: "config"
    - from: "Dockerfile"
      to: "src/store_predict/main.py"
      via: "CMD python -m store_predict.main"
      pattern: "store_predict\\.main"
---

<objective>
Create the NiceGUI app skeleton with page routing and Docker deployment.

Purpose: Make the app runnable both locally and in Docker, with a landing page and upload placeholder. This completes Phase 1's "runnable skeleton" goal.
Output: Working NiceGUI app at localhost:8080, Dockerfile + docker-compose.yml.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-drr-table/01-RESEARCH.md
@.planning/phases/01-foundation-drr-table/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NiceGUI app skeleton with landing page and upload placeholder</name>
  <files>
    src/store_predict/main.py
    src/store_predict/ui/layout.py
    src/store_predict/ui/pages/upload.py
  </files>
  <action>
    1. Create src/store_predict/ui/layout.py:
       - A shared layout context manager or function that provides:
         - Header with app title "StorePredict" styled with Tailwind (text-2xl font-bold, dark background)
         - Simple navigation links: Home (/), Upload (/upload)
         - Use `ui.header()` and `ui.link()` from NiceGUI 3.x
       - Keep styling minimal (Tailwind utility classes via .classes())
       - Use `from __future__ import annotations`

    2. Create src/store_predict/ui/pages/upload.py:
       - `@ui.page("/upload")` decorator
       - Use the shared layout
       - Show placeholder text: "Upload page - coming in Phase 2"
       - Add a `ui.label` explaining accepted formats: "Accepts RVTools (.xlsx), LiveOptics (.xlsx, .csv)"
       - Do NOT implement actual upload logic yet (that is Phase 2)

    3. Create src/store_predict/main.py:
       - Import ui/pages/upload to register the /upload route (just the import is enough for NiceGUI to register the page)
       - `@ui.page("/")` for the landing page:
         - Use the shared layout
         - Show app title "StorePredict" with subtitle "Dell PowerStore DRR Sizing Tool"
         - Brief description of what the tool does
         - A button or link to go to /upload using `ui.navigate.to("/upload")` (NOT `ui.open()` which was removed in 3.0)
       - `def main() -> None:` calling `ui.run(title="StorePredict", port=8080, storage_secret="change-me-in-production", reload=False)`
       - `if __name__ == "__main__": main()`
       - Do NOT use [project.scripts] entry point (NiceGUI bug)
       - Import config values (APP_TITLE, APP_PORT) from config.py

    4. Verify locally: `python -m store_predict.main` starts without errors (ctrl-C to stop).
    5. Run `rtk ruff check src/` and `rtk mypy src/` — fix any issues.
  </action>
  <verify>
    - `python -m store_predict.main &` starts server, `curl -s http://localhost:8080 | grep -i storepredict` finds the title, then kill the process
    - `rtk ruff check src/` zero errors
    - `rtk mypy src/` zero errors (with nicegui ignore_missing_imports already configured)
  </verify>
  <done>
    NiceGUI app starts on port 8080, landing page shows at /, upload placeholder at /upload, navigation works between pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Dockerfile and docker-compose.yml, verify full stack</name>
  <files>
    Dockerfile
    docker-compose.yml
  </files>
  <action>
    1. Create Dockerfile using the research template:
       ```
       FROM python:3.12-slim
       WORKDIR /app
       COPY pyproject.toml .
       COPY src/ src/
       COPY samples/DRR.csv samples/DRR.csv
       RUN pip install --no-cache-dir .
       EXPOSE 8080
       CMD ["python", "-m", "store_predict.main"]
       ```

    2. Create docker-compose.yml:
       ```yaml
       services:
         app:
           build: .
           ports:
             - "8080:8080"
           environment:
             - STORAGE_SECRET=change-me-in-production
           restart: unless-stopped
       ```

    3. Build and test: `docker compose up --build -d`, wait for startup, `curl http://localhost:8080`, then `docker compose down`.

    4. Run final verification of ALL Phase 1 success criteria:
       - `rtk pytest` all green
       - `rtk ruff check .` zero errors
       - `rtk mypy src/` zero errors
       - App runs locally
       - Docker works
  </action>
  <verify>
    - `docker compose up --build -d` succeeds
    - `curl -s http://localhost:8080 | grep -i storepredict` returns content
    - `docker compose down` cleans up
    - `rtk pytest` all green
  </verify>
  <done>
    Docker image builds, container starts and serves the app on port 8080. All Phase 1 success criteria met: pytest passes, app shows a page, docker compose works.
  </done>
</task>

</tasks>

<verification>
Phase 1 complete verification:
1. `rtk pytest` — all tests pass, >80% coverage on pipeline/ and services/
2. `python -m store_predict.main` — starts web server, shows landing page
3. `docker compose up --build` — builds and serves app
4. `rtk ruff check .` — zero lint errors
5. `rtk mypy src/` — zero type errors
6. Pipeline package has zero imports from UI: `grep -r "from store_predict.ui" src/store_predict/pipeline/` returns nothing
</verification>

<success_criteria>
- NiceGUI app starts locally and shows landing page at /
- Upload placeholder page accessible at /upload
- Docker image builds and container serves app on port 8080
- All tests from Plan 01 still pass
- ruff and mypy clean across entire codebase
- NFR-2.4 satisfied: pipeline/ has no ui/ imports
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-drr-table/01-02-SUMMARY.md`
</output>
