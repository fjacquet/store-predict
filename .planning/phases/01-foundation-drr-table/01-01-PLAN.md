---
phase: 01-foundation-drr-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/store_predict/__init__.py
  - src/store_predict/config.py
  - src/store_predict/pipeline/__init__.py
  - src/store_predict/pipeline/models.py
  - src/store_predict/services/__init__.py
  - src/store_predict/services/drr_table.py
  - src/store_predict/ui/__init__.py
  - src/store_predict/ui/pages/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_drr_table.py
  - tests/test_models.py
autonomous: true

must_haves:
  truths:
    - "DRR.csv loads correctly with all 30 workload categories"
    - "PostgreSQL entry with embedded newline parses without error"
    - "Trailing junk rows in DRR.csv are filtered out"
    - "Unknown category lookups return default DRR of 5.0"
    - "Multi-workload conservative ratio returns the minimum DRR"
    - "All DRR ratios are positive (no division-by-zero risk)"
    - "ruff check and mypy pass with zero errors"
    - "pytest passes with all DRR and model tests green"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, dependencies, tool config (ruff, mypy, pytest)"
      contains: "nicegui>=3.4"
    - path: "src/store_predict/pipeline/models.py"
      provides: "VMRecord dataclass, FileFormat enum"
      exports: ["VMRecord", "FileFormat"]
    - path: "src/store_predict/services/drr_table.py"
      provides: "DRREntry dataclass, DRRTable service"
      exports: ["DRREntry", "DRRTable"]
    - path: "src/store_predict/config.py"
      provides: "Project paths and defaults"
      contains: "DRR_CSV_PATH"
    - path: "tests/test_drr_table.py"
      provides: "DRR service tests"
      min_lines: 40
    - path: "tests/test_models.py"
      provides: "Data model tests"
      min_lines: 20
  key_links:
    - from: "tests/conftest.py"
      to: "src/store_predict/services/drr_table.py"
      via: "DRRTable.from_csv fixture"
      pattern: "DRRTable\\.from_csv"
    - from: "tests/test_drr_table.py"
      to: "samples/DRR.csv"
      via: "conftest fixture loading real CSV"
      pattern: "drr_table"
    - from: "src/store_predict/config.py"
      to: "samples/DRR.csv"
      via: "DRR_CSV_PATH constant"
      pattern: "DRR\\.csv"
---

<objective>
Create the Python project structure, data models, DRR table service, and test suite for StorePredict.

Purpose: Establish the foundational package layout with typed models and a fully tested DRR reference data service. This is the base everything else builds on.
Output: Working Python package with pyproject.toml, pipeline/models, services/drr_table, and passing tests.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-drr-table/01-RESEARCH.md
@samples/DRR.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure with pyproject.toml, package layout, config, and data models</name>
  <files>
    pyproject.toml
    src/store_predict/__init__.py
    src/store_predict/config.py
    src/store_predict/pipeline/__init__.py
    src/store_predict/pipeline/models.py
    src/store_predict/services/__init__.py
    src/store_predict/ui/__init__.py
    src/store_predict/ui/pages/__init__.py
    tests/__init__.py
  </files>
  <action>
    1. Create pyproject.toml using the EXACT template from 01-RESEARCH.md:
       - build-system: setuptools>=75.0 with _legacy backend
       - dependencies: nicegui>=3.4,<4.0, pandas>=2.2,<4.0, openpyxl>=3.1.2, reportlab>=4.0
       - dev extras: pytest>=8.0, pytest-cov>=5.0, ruff>=0.9, mypy>=1.10, pandas-stubs>=2.2
       - docs extras: mkdocs, mkdocs-material
       - tool.setuptools.packages.find where=["src"]
       - ruff: target-version py312, line-length 99, select E/W/F/I/N/UP/B/SIM/TCH/RUF, isort known-first-party=store_predict
       - mypy: strict=true, python_version 3.12, overrides for tests (no strict defs), nicegui/reportlab (ignore missing imports)
       - pytest: testpaths=["tests"], addopts="--cov=store_predict --cov-report=term-missing"
       - Do NOT add [project.scripts] entry points (NiceGUI bug per research)

    2. Create all package directories and __init__.py files:
       - src/store_predict/__init__.py: just `__version__ = "0.1.0"`
       - src/store_predict/pipeline/__init__.py: empty with docstring
       - src/store_predict/services/__init__.py: empty with docstring
       - src/store_predict/ui/__init__.py: empty with docstring
       - src/store_predict/ui/pages/__init__.py: empty with docstring
       - tests/__init__.py: empty

    3. Create src/store_predict/config.py:
       - `from pathlib import Path`
       - `PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent` (points to repo root)
       - `SAMPLES_DIR = PROJECT_ROOT / "samples"`
       - `DRR_CSV_PATH = SAMPLES_DIR / "DRR.csv"`
       - `DEFAULT_DRR = 5.0`
       - `APP_TITLE = "StorePredict"`
       - `APP_PORT = 8080`

    4. Create src/store_predict/pipeline/models.py:
       - Use `from __future__ import annotations`
       - `FileFormat` enum: RVTOOLS="rvtools", LIVEOPTICS_XLSX="liveoptics_xlsx", LIVEOPTICS_CSV="liveoptics_csv"
       - `VMRecord` frozen dataclass: vm_name (str), os_name (str), provisioned_mib (float), in_use_mib (float), source_format (FileFormat), datacenter (str=""), cluster (str=""), is_template (bool=False), is_powered_on (bool=True)
       - All fields typed, all with sensible defaults where appropriate

    5. Run `pip install -e ".[dev]"` to install the package in dev mode.
  </action>
  <verify>
    - `python -c "from store_predict.pipeline.models import VMRecord, FileFormat; print('OK')"` prints OK
    - `python -c "from store_predict.config import DRR_CSV_PATH; print(DRR_CSV_PATH.exists())"` prints True
    - `rtk ruff check src/` passes with zero errors
    - `rtk mypy src/store_predict/config.py src/store_predict/pipeline/models.py` passes
  </verify>
  <done>
    Python package installs, all imports work, models and config are typed and lint-clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement DRR table service and full test suite</name>
  <files>
    src/store_predict/services/drr_table.py
    tests/conftest.py
    tests/test_drr_table.py
    tests/test_models.py
  </files>
  <action>
    1. Create src/store_predict/services/drr_table.py using the research template:
       - `DRREntry` frozen dataclass: category (str), subcategory (str), ratio (float)
       - `DRRTable` class with:
         - `__init__(self, entries: list[DRREntry])` storing entries and building _lookup dict keyed by (category, subcategory)
         - `from_csv(cls, path: Path) -> DRRTable` classmethod:
           - Use `pd.read_csv(path, sep=";", names=["category","subcategory","ratio"], skiprows=1, quoting=csv.QUOTE_ALL, engine="python")`
           - `df.dropna(subset=["category"])` to remove empty rows
           - `pd.to_numeric(df["ratio"], errors="coerce")` then `df.dropna(subset=["ratio"])` to remove junk
           - Strip whitespace from category and subcategory: `df[col].str.strip()`
           - Build DRREntry list from iterrows
         - `get_ratio(self, category: str, subcategory: str) -> float` returning lookup or 5.0 default
         - `get_conservative_ratio(self, workloads: list[tuple[str, str]]) -> float` returning min ratio or 5.0 for empty
         - `categories` property returning sorted unique category names
         - `entries` property returning copy of entries list
         - `__len__` returning entry count
       - Use `from __future__ import annotations`, full type annotations

    2. Create tests/conftest.py:
       - `sample_drr_path` fixture returning `Path(__file__).parent.parent / "samples" / "DRR.csv"`
       - `drr_table` fixture using `DRRTable.from_csv(sample_drr_path)`

    3. Create tests/test_drr_table.py with these tests (from research, plus extras):
       - `test_drr_table_loads_30_entries` — assert len == 30
       - `test_postgresql_entry_parsed_correctly` — Database/PostgreSQL ratio == 1.5
       - `test_unknown_reducible_default` — Unknown (Reducible) has ratio 5.0
       - `test_missing_category_returns_default` — nonexistent key returns 5.0
       - `test_conservative_ratio_returns_minimum` — multi-workload returns min
       - `test_conservative_ratio_empty_returns_default` — empty list returns 5.0
       - `test_all_ratios_positive` — every entry.ratio > 0
       - `test_categories_returns_sorted_unique` — categories property is sorted, no duplicates
       - `test_entries_returns_copy` — modifying returned list doesn't affect internal state

    4. Create tests/test_models.py:
       - `test_vmrecord_creation` — create VMRecord with all fields, assert values
       - `test_vmrecord_frozen` — attempt to modify raises FrozenInstanceError
       - `test_vmrecord_defaults` — create with only required fields, check defaults
       - `test_fileformat_values` — each enum has expected string value
       - `test_vmrecord_equality` — two identical VMRecords are equal

    5. Run full quality checks:
       - `rtk ruff check src/ tests/`
       - `rtk ruff format src/ tests/`
       - `rtk mypy src/`
       - `rtk pytest`
       Fix any issues until all pass.
  </action>
  <verify>
    - `rtk pytest` shows all tests passing with >80% coverage on store_predict/services/
    - `rtk ruff check src/ tests/` zero errors
    - `rtk mypy src/` zero errors (or only expected pandas-stubs edge cases with targeted ignores)
  </verify>
  <done>
    DRR.csv loads 30 entries, PostgreSQL embedded newline handled, junk rows filtered, all lookup methods work correctly, all tests green, lint and type checks pass.
  </done>
</task>

</tasks>

<verification>
- `python -c "from store_predict.services.drr_table import DRRTable; from store_predict.config import DRR_CSV_PATH; t = DRRTable.from_csv(DRR_CSV_PATH); print(f'{len(t)} entries loaded')"` prints "30 entries loaded"
- `rtk pytest` all green
- `rtk ruff check .` zero errors
- `rtk mypy src/` zero errors
</verification>

<success_criteria>
- pyproject.toml exists with all deps and tool config
- Package installs with `pip install -e ".[dev]"`
- DRRTable.from_csv loads exactly 30 entries from samples/DRR.csv
- All 9+ DRR tests and 5+ model tests pass
- ruff and mypy pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-drr-table/01-01-SUMMARY.md`
</output>
